"""[v1.4 파이프라인 ①~⑬] 노드 배선 — 지시문(2026-09-18) 구현.

🔴 **순서가 이 봇의 정체다**(CLAUDE.md "페이블처럼 분석한다").
   판단을 먼저 적는다 → 무엇을 찾을지 먼저 정한다 → 그것만 찾는다 →
   찾았는지 기계가 센다 → 그 결과로만 확률이 움직인다.

🔴 **노드는 서로 부르지 않는다.** ①~⑬은 각각 `state -> state` 순수 함수이고,
   순서와 분기는 오직 `run.py` 한 파일이 정한다. 노드 안에서 다른 노드를
   import 하거나 호출하면 **배선 오류**다(계약 테스트가 잡는다).

🔴 **하나의 상태 객체.** 모든 노드는 `state.State` 하나만 읽고 쓴다.
   노드는 자기 키(`n0X_*`)만 쓴다 — 다른 노드 키를 고치면 배선 오류다.

⚠️ 패키지 이름이 `pipeline` 이 아니라 `flow` 인 이유: `app/pipeline.py` 라는
   **모듈 파일이 이미 있다**(운영 핵심 6,200줄). 최상위에 `pipeline/` 패키지를
   만들면 `from app.pipeline import …` 와 `from pipeline import …` 가 공존해
   실행 위치에 따라 임포트가 갈린다. 지시문의 내부 구조는 그대로 두고 뿌리만 옮겼다
   (2026-09-18 사용자 승인).
"""
