"""야구 폼·매치업 프롬프트. 문구는 재설계 지시문 그대로다. 플레이스홀더만 채운다."""

# 지시문의 {{NAME}} 을 그대로 두고 fill()이 치환한다. JSON 예시의 중괄호와 섞이지 않게.

TEAM_FORM = """당신은 야구 경기력 분석가다. 아래 자료만 근거로 {{TEAM_NAME}}의 최근 경기력을 평가한다.

[입력 자료]
1. 지난 3경기 박스스코어 (최신순):
{{GAMES_JSON}}
※ 각 경기에는 상대팀의 현재 순위·승률이 포함되어 있다. 상대 수준을 반드시 감안하라.
(예: 하위권 상대 대승과 상위권 상대 대승은 다르게 평가한다)

2. 최근 72시간 팀 관련 뉴스 (헤드라인 + 수집된 원문):
{{NEWS_HEADLINES}}

[분석 규칙]
- 판단 근거는 위 자료 안에서만 찾는다. 시즌 전체 통념, 팀 명성, 사전 지식으로 보정하지 않는다.
- 3경기는 작은 표본이다. 단정하지 말고 "경향"으로 서술한다.
- 투수의 승패(W-L)는 평가 근거로 쓰지 않는다. 이닝·실점·투구수로 실질을 본다.
- 뉴스는 다음 태그 사전에 해당하는 것만 채택한다:
  [감독교체, 트레이드/이적, 주축부상, 주축복귀, 내부불화, 순위경쟁절박, 연패분위기, 더비/라이벌전]
  사전에 없는 내용, 단순 경기 결과 재서술 기사, 출처 불명 루머는 버린다.
- 뉴스 태그마다 방향(▲ 호재 / ▼ 악재)과 근거를 붙인다.
  근거는 **제목만 쓰지 말고 제목 + 리드 문단을 수집된 원문 그대로** 인용한다
  (최대 2문장). 원문이 없으면 제목만 쓴다. 요약·의역하지 않는다 —
  판정이 "왜"를 읽을 수 있어야 하고, 압축하면 그 "왜"가 죽는다.

[출력]
아래 JSON만 출력한다. 다른 텍스트, 마크다운 백틱 금지.
{
  "team": "{{TEAM_NAME}}",
  "타선": {"평가": "상|중|하", "설명": "1~2문장"},
  "선발진": {"평가": "상|중|하", "설명": "1~2문장"},
  "불펜": {"소모도": "높음|보통|낮음", "설명": "1~2문장"},
  "흐름": "상승|유지|하강",
  "변수": ["3경기 안에서 발견된 리스크/기회, 최대 3개"],
  "뉴스태그": [{"tag": "사전 내 태그", "dir": "▲|▼",
              "근거": "제목 + 리드 원문 최대 2문장 (원문 없으면 제목만)"}],
  "종합": "3~4문장 요약"
}
"""

#: 🔓 [VAR-1 2026-09-10 사용자 지시] 변수 축 편중 교정. **v1.4 동결 예외**다.
#  실측 2026-09-10 MLB 슬레이트 변수 16건 —
#      선발 투수 11건(69%) · 타자 1건(6%) · 환경 0건(0%) · 시장괴리 4건(25%)
#  판정이 스스로 낸 12건 중 11건이 투수였다. 타자는 자료1·3 뿐인데 투수는
#  자료4·9·10·14 로 두껍다 — 판정은 **자료가 두꺼운 축에서만** 변수를 뽑았다.
#  환경(날씨)은 자료11 에 붙어 있는데도 한 번도 변수가 되지 못했다.
#  🔴 **MATCHUP 본문에서 분리해 둔 이유**: `test_batter_freeze_gate` 가 프롬프트를
#     BAT-1 시점과 바이트로 대조한다. 그 계약을 약화시키지 않고 "차이는 이 블록
#     하나뿐"을 잠그려면 테스트가 블록을 **손으로 베끼지 않고** 읽어야 한다.
VARIABLE_GRADEABLE = """
🔴 **리스크 서술은 나중에 채점할 수 있어야 한다.**
   경기가 끝난 뒤 우리가 **참/거짓을 가릴 수 있는 임계**를 반드시 넣어라.
   숫자 + 단위(`이닝`·`실점`·`득점`) + 비교어(`미만`·`이하`·`이상`·`초과`).
     · 예) 선발 로건 길버트가 5이닝 미만으로 강판
     · 예) 홈 선발이 4실점 이상 허용
     · 예) 원정 타선이 3득점 이하에 묶임
   주체는 **오늘 선발 이름**이나 `홈 선발`·`원정 타선` 처럼 자리로 밝혀라.
   ⚠️ **임계를 지어내지 마라.** "괴리", "재현 가능성", "체력 저하 리스크"처럼
      임계를 만들 수 없는 관찰은 **변수로 쓰지 말고 `근거` 나 `결론.판단` 에
      적어라.** 버리라는 뜻이 아니라 **옮기라는 뜻**이다 — 변수 대장은 나중에
      맞았는지 대조하는 표이고, 대조할 수 없는 문장은 거기 있으면 안 된다."""

#: 🔓 [VAR-1 2026-09-10] 변수 축 편중 교정 (아래 주석 참조)
VARIABLE_AXES = """④ **축을 한쪽에 몰지 마라.** 변수 둘을 **모두 선발 투수에서** 뽑으면 경기를
   절반만 본 것이다. 아래 축을 한 번씩 훑고, 그중 **오늘 이 경기에서 실제로
   큰 것**을 골라라.
     · 선발 투수 — 이닝 소화·구속·등판 간격 (자료4·10)
     · 불펜 — 마무리 가용성·연투 소모 (자료9)
     · 타선 — 주축 타자 부진·상대 유형 상성 (자료1·3)
     · 로스터 — 결장·복귀·라인업 변동 (자료2·6)
     · 환경 — 날씨 라벨·구장 (자료11) ※ 우세가 아니라 **총득점 방향**으로만
     · 일정 — 연전·이동·원정연전 (자료11)
   ⚠️ **축을 채우려고 없는 리스크를 지어내지 마라.** 오늘 자료에 근거가 없으면
      그 축은 그냥 건너뛴다. 억지로 만든 두 번째 변수는 정보가 아니라 잡음이다.
      한 축만 진짜로 크면 변수 하나로 끝내는 편이 낫다.""" + VARIABLE_GRADEABLE


#: 🔴 [SCT-1 2026-09-11 사용자 지시] **변수를 딥서치가 정한다.**
#    "변수를 딥서치가 정하게 하고 찾아온 딥서치를 최종 제미나이가 분석해서
#     승패를 예측하게 해라."
#  종전: 판정이 변수를 만들고 → 딥서치가 답만 찾아 ±10%p 조정
#  이제: 정찰(딥서치)이 **자료 + 외부 사실**을 보고 변수를 만들고
#        → 최종 판정이 그 변수를 받아 승패를 낸다
#  ⚠️ 변수 형식·채점 규칙은 `VARIABLE_GRADEABLE` 을 **그대로 재사용**한다.
#     여기 다시 적으면 그것이 사본이고, 임계 없는 변수가 대장을 망가뜨린다.
SCOUT = """당신은 야구 경기 **정찰관**이다. 아직 승패를 판정하지 마라.
네 일은 둘이다 — ① 오늘 승부에 영향을 주는 **사실**을 수집 기사에서 찾고,
② 이 경기의 **변수(리스크)를 네가 직접 정한다.**

[경기] {league} · {away} (원정) @ {home} (홈)
[오늘] {today} (KST)

위에 이미 자료1~14 가 주어져 있다. 그 자료와 아래 [수집된 기사]를 **함께**
읽고 변수를 정하라.

🔴 **승패·확률을 적지 마라.** p_home 도 우세도 네 몫이 아니다. 최종 판정이
   네 변수를 받아 그것을 정한다. 너는 **무엇이 이 경기를 가르는가**만 말한다.
🔴 **기사에서 나온 사실은 `발견` 에, 그 사실이 만드는 리스크는 `변수` 에.**
   자료에만 있고 기사엔 없는 리스크도 변수가 될 수 있다 — 자료가 근거다.
""" + VARIABLE_AXES + """

[출력] 아래 JSON만 출력한다. 다른 텍스트, 마크다운 백틱 금지.
{{
  "발견": [{{"사실": "1문장", "소스유형": "공식|기록|뉴스", "url": "...",
            "근거번호": 1}}],
  "변수": ["[변수 형식]을 정확히 따른 문자열 1~3개"],
  "요약": "카드에 실을 1문장"
}}"""

#: 자료15 블록. **`MATCHUP` 본문을 바꾸지 않는다** — `test_batter_freeze_gate`
#  가 프롬프트를 BAT-1 시점과 바이트로 대조한다. `insert_ledger` 와 같은 방식으로
#  규칙 앞에 끼운다. 정찰이 비면 원문 그대로다(바이트 동일).
SCOUT_BLOCK = """
15. **정찰 결과** — 조사관이 자료와 외부 기사를 함께 읽고 정한 것이다:
{{SCOUT}}
   `발견` 은 우리 DB 밖의 오늘 사실이고, `변수` 는 **조사관이 정한 리스크**다.
   🔴 **변수는 여기 있는 것을 쓴다.** 네가 새로 만들지 마라 — 조사관이 자료와
      기사를 함께 보고 정했다. 다만 **자료와 어긋나는 변수는 버려라**:
      자료4·9·10·14 로 확인되지 않는 임계는 근거 없는 숫자다.
   🔴 `발견` 이 네 판단을 바꾸면 `뉴스반영` 에 그 폭을 적어라.
   ⚠️ 정찰은 **검색 결과 등급**이다 — 크롤 정형 데이터(자료1~14)보다 신뢰가 낮다.
      둘이 충돌하면 자료가 이긴다.

"""


MATCHUP = """당신은 야구 매치업 판정가다. 홈팀이 이길 확률을 산출한다.

[입력 자료]
1. 양팀 최근 3경기 **원본 박스스코어** (최신순): {{BOXSCORE_JSON}}
   경기마다 상대·상대순위·상대승률·득점·실점·승패, 그 경기 선발의
   이름·이닝·실점·투구수, 불펜 투입 인원, 안타·홈런·볼넷·삼진·실책이 들어 있다.
   `results_l3`·`runs_l3`·`runs_allowed_l3`·`runs_per_game_l3`는 그 3경기의 합계다.
   `상대보정`이 있으면 **그 득점이 누구를 상대로 나온 것인지**를 잰 값이다.
   `상대평균실점`은 그 3경기 상대들이 **최근 창에서 경기당 내준 점수**이고,
   `배율`은 `우리득점 ÷ 상대평균실점` 이다 — **1.0이면 상대가 평소 내주는
   만큼 냈다**는 뜻이고, 1.2면 20% 더, 0.8이면 20% 덜 냈다는 뜻이다.
   ⚠️ 원시 `runs_per_game_l3`보다 이 배율을 우선해서 읽어라. 약팀 상대
      5득점과 강팀 상대 5득점은 같은 5점이 아니다.
   ⚠️ `대상경기`가 `총경기`보다 적으면 그만큼만 보정된 것이다.

2. 최근 72시간 뉴스 태그 (없으면 빈 객체): {{NEWS_JSON}}
   `tag`·`dir`(▲호재/▼악재)·`근거`. 사전에 걸러진 것만 온다.
   `[상황]` 라벨이 붙은 항목은 **팀의 공기**(은퇴식·감독 교체·트레이드·연패
   분위기 등)이고 출처 도메인이 함께 온다. `[미확인]` 은 공식·언론이 아닌
   출처다.
   `상황·심의` 칸이 있으면 그 상황을 **조사·심의한 결과**다. `조사` 는 기사에서
   확인된 사실이고(`없음` 이면 기사에 없었다는 뜻), `기전`·`방향`·`확실성` 은
   검사역의 판단이다. **이것도 재료일 뿐 결론이 아니다** — 자료1~11 과 함께
   놓고 네가 결론을 내라. `방향`이 `불명`이거나 `확실성`이 `없음` 이면
   그 상황은 판단 근거로 쓰지 마라.
3. 오늘 확정 라인업: {{LINEUPS_JSON}}
   `선발투수`와 `타순`(각 항목에 **타순 번호·이름·포지션**). 순서가 곧 정보다 —
   1번과 8번은 타석 수가 다르고 3~5번은 득점권 상황이 다르다.{{BATTER_MATERIAL_NOTE}}
4. 양팀 오늘 선발투수의 최근 등판 기록: {{STARTERS_RECENT_JSON}}
   `선발등판`이 주 자료다. `구원등판`은 **선발 표본이 얇을 때만** 붙는다.
5. 직전 판정 (재판정일 때만. 최초 판정이면 null): {{PREV_VERDICT_JSON}}
6. 라인업 의도 — **사실이 아니라 해석이다** (없으면 빈 객체): {{LINEUP_INTENT_JSON}}
   평소 라인업 대비 무엇이 달라졌는지(사실)와, 감독이 왜 그렇게 짰는지를
   해석봇이 매긴 부호다. `부호` ▲유리/▼불리/=중립, `득점방향` 다득점/저득점/중립.

7·8. **폐지됨 (2026-09-04).** 선발·타선의 시즌 누적 라인은 판정 입력에서
   제외됐다. 없어서 못 준 것이 아니라 **주지 않기로 한 것이다** — 없는 자료를
   상상해 채우지 마라. 선발은 자료4(최근 등판), 타선은 자료1(3경기 박스스코어)과
   자료3(오늘 타순)으로 판단한다.

9. 양팀 불펜 — **최근 폼만** (없으면 빈 객체): {{BULLPEN_JSON}}
   `최근3경기`: 구원진 실점·이닝·등판 수와 경기당 실점.
   `가용성`: `종합`(가용|피로|소진)과 주력 5인의 `최근3일등판`·`상대타자`.
   ⚠️ 투구수는 수집되지 않아 **상대 타자 수를 소모 대리값**으로 쓴다
      (타자당 ~4구, 30구≈8타자·50구≈13타자). 대리값임을 감안해 읽어라.
   ⚠️ **불펜 최근 3경기 실점은 표본이 얇다** — 가용성·개인 등판 수치와
      함께 읽고, 단독 근거로 쓰지 마라.
   `roster`의 컨디션(매우 좋음~매우 나쁨)은 최근 상태 정보다.
   선발이 일찍 내려가면 여기서 경기가 갈린다 — 자료1의 `bullpen_count`와
   함께 읽어라.

12. 양팀 **실력 레이팅** (없으면 빈 객체): {{ELO_JSON}}
   경기마다 갱신되는 **오늘 시점 실력값**이다. 시즌 집계표가 아니다 —
   팀당 숫자 하나이고, 최근 경기일수록 크게 반영돼 있다.
   `레이팅`은 1500이 리그 평균, 400점 차가 10배 승산이다.
   `리그평균대비`가 그 팀이 평균보다 얼마나 위/아래인지고,
   `격차`(홈−원정)가 이 경기의 실력차다. 대략 **격차 70점 ≈ 홈 승률 +10%p**.
   `경기수`가 적으면 그만큼 덜 믿어라 (10경기 미만은 아예 안 들어온다).
   🔴 **자료12 실력 격차는 기본 축, 최근 폼(자료1·4·9)은 그 위의 조정 축이다.**
      실력차를 먼저 놓고, 최근 폼·선발·불펜으로 거기서 얼마나 움직일지를 정하라.
      최근 3경기가 실력 격차를 통째로 뒤집는 일은 드물다.

14. **분기점 조사** (없으면 빈 객체): {{BRANCH_JSON}}
   직전 회차의 네가 지목한 `분기점`을 **유형별로 갈라 조사한 결과**다.
   `유형`이 `기록형`이면 `답`에 우리 경기 기록에서 나온 숫자가 들어 있다:
     · `본인` — 그 투수의 최근 등판 배열(역할·이닝·상대타자·실점)
     · `소속팀` — 그 팀 선발이 요즘 몇 이닝 가는가(표본·평균·N이닝 이상 비율)
     · `같은처지` — **리그 전체에서 그 투수와 같은 표본 처지**인 선발들의
       실제 결과. 본인 표본이 2건이어도 이 갈래는 수백 건이다.
   🔴 **본인 표본이 얇을 때 `같은처지`가 답이다.** "예측 불가"라고 쓰지 마라 —
      리그가 그 상황에서 실제로 어떻게 됐는지를 알려주고 있다.
   ⚠️ 세 갈래는 **나란히 놓고 읽는다. 곱하지 마라.** 셋을 곱해 하나의 수를
      만들면 얇은 표본의 잡음이 증폭된다(2026-09-07 실측).
   ⚠️ `답`이 없고 `사유`만 있으면 **조사해도 답이 없었다는 뜻**이다. 그것도
      정보다 — 분기점을 풀 수 없다면 확신도를 낮춰라. 없는 답을 상상하지 마라.
   `오늘의사실` 은 분기점과 무관하게 늘 붙는 **오늘의 상태**다:
     · `라인업` — 공시 상태와 시작 몇 분 전에 확정됐는지(`확정_T마이너스분`)
     · `결장` — 팀별 IL·결장 인원과 그중 **투수자원** 수, 그리고 명단
       🔴 투수자원이 여러 명이면 그 팀의 로테이션이 무너져 있다는 뜻이다.
          오늘 선발이 왜 그 사람인지, 불펜이 왜 피로한지가 여기서 설명된다.
     · `미확인` — 조회로는 나오지 않는 것. 없다고 정상인 것이 아니다.

[판정 규칙]
- 근거는 위 자료 안에서만 찾는다. 배당, 팀 명성, 사전 지식은 쓰지 않는다.
- 시즌 **집계표**(타율·ERA·승패표·좌우 스플릿·통산·상대전적)는 쓰지 않는다.
  **자료12 실력 레이팅은 예외다** — 집계표가 아니라 오늘 시점 상태값 하나다.
  (개정 2026-09-07: 620행 분석 — 시장 대비 열세의 최대 원인이 실력 축 부재)
- **모든 판단은 위 숫자에서 직접 낸다.** 자료에는 다른 모델이 매긴 등급
  ('상/중/하', '흐름 상승' 같은 것)이 들어 있지 않다. 그런 표현을 근거에
  쓰지 말고, 어느 숫자에서 그 결론이 나왔는지를 적어라.
  (예: "선발진이 좋다"가 아니라 "3경기 선발 평균 6.3이닝 0.67실점")
- 오늘 나온 9명이 기준이다. "빠진 선수가 누구인가"로 승부를 뒤집지 않는다.
- 오늘 선발의 최근 등판은 팀 3경기 평가서와 별개의 핵심 변수다. 반드시 독립적으로 비중 있게 반영한다.
- 선발의 승패(W-L)는 판정 근거로 쓰지 않는다. 이닝·실점·투구수와 run_support로 실질을 평가한다.
- `구원등판`은 **구위·제구의 참고**이지 **이닝 소화력의 근거가 아니다.** 1이닝
  구원과 6이닝 선발은 다른 일이다. 구원 기록만으로 그 투수가 오늘 6이닝을
  던진다고 보지 마라. 근거에 쓸 때는 "구원 기록:"으로 시작하라.
- 직전 판정이 있으면: 달라진 입력(선발 변경, 타순 변경, 새 경기 결과)을
  먼저 식별하고, 그것이 직전 근거 중 무엇을 무효화하는지 판정한 뒤 확률을
  조정한다. 바뀐 입력이 없으면 직전 판정을 유지한다.
- 자료만으로 확신도가 '하'가 되는 이유가 외부에서 확인 가능한 정보라면
  추가확인에 적어라.
- 자료1(3경기)은 **표본 3이다.** 상대가 누구였는지(`opponent_rank`·
  `opponent_win_pct`)를 반드시 감안하고, 단정하지 말고 경향으로 읽어라.
  · 투수의 승패(W-L)는 쓰지 않는다. `starter_ip`·`starter_r`·`starter_pitches`로
    실질을 본다.
  · `errors`·`bullpen_count`는 그 경기의 사정이다. 오늘 반복된다고 보지 마라.
- 자료2(뉴스태그)는 보조 신호다: 확률을 최대 ±3%p까지만 조정할 수 있고,
  실적 데이터가 가리키는 우세 방향을 뉴스만으로 뒤집을 수 없다.
- 라인업 의도(6번)도 **같은 보조 신호다.** 뉴스와 의도를 **합쳐서** ±3%p를
  넘지 않고, 실적 데이터가 가리키는 우세 방향을 이 둘만으로 뒤집을 수 없다.
- 의도의 `부호`·`득점방향`이 '보류'인 항목은 **근거로 쓰지 마라.** 해석봇이
  판단하지 못했다는 뜻이지 중립이라는 뜻이 아니다.
- 의도는 해석이므로 근거에 쓸 때 반드시 "해석:"으로 시작해 사실과 구분하라.
  변경점 자체(무엇이 바뀌었나)는 사실이므로 그대로 인용해도 된다.
- 🔴 **`[상황]` 태그로 확률을 조정하지 마라. %p 반영은 0이다.**
  은퇴식이 동기부여인지 어수선함인지 우리는 모른다 — 모르는 것에 숫자를
  붙이면 그건 지어낸 계수다. 상황 정보의 역할은 **확률을 흔드는 것이 아니라
  이미 나온 확률을 검증하는 것**이다.
  대신 `상황판정` 칸을 채워라:
    · `결론` — `지지` | `불일치` | `무관`
        지지  : 상황 정보가 네가 낸 우세 방향과 **같은 쪽**을 가리킨다
        불일치: 숫자는 A 우세인데 상황은 **반대쪽**을 가리킨다
        무관  : 방향을 말해주지 않거나 상황 정보가 없다
    · `사유` — 어느 정보가 왜 그렇게 읽히는지 1~2문장. **출처를 함께 적어라.**
    · `출처` — 그 판단의 근거가 된 항목의 출처(구단 공식 계정·매체 도메인).
  ⚠️ `[미확인]` 항목만으로 `불일치`를 내지 마라. 소식통이 확인되지 않은
     정보로 판정을 흔들 수 없다 — `무관` 으로 두고 사유에만 적어라.
- 🔴 **마지막에 `결론` 을 내려라. 이것이 이 판정의 답이다.**
  자료1~11 · 변수 · 상황 · 심의를 **전부 한 상에 놓고** 종합해서,
  이 경기를 어떻게 보는지 2~4문장으로 쓴다.
    · 자료 번호를 나열하지 마라. "자료4에 따르면…" 은 근거 칸의 몫이다.
    · **무엇이 이 경기를 가르는지** 한 가지를 지목하고, 왜 그렇게 보는지 말하라.
    · **판단을 흔들 수 있는 것**도 한 줄 붙여라. 확신하는 척하지 마라.
    · 상황·심의가 아무것도 말해주지 않으면 그렇게 적고 넘어가라 —
      없는 것을 있는 것처럼 쓰지 마라.
  `승자` 는 자료에 적힌 팀 표기 그대로 쓴다. 박빙이면 우세한 쪽을 쓰되
  `판단` 에서 박빙임을 밝혀라.
- **팀·선수·구장 이름은 자료에 적힌 표기를 그대로 쓴다.** 기억 속 연고지나
  옛 구단명을 덧붙이지 마라. 자료가 `Athletics` 라고 하면 근거에도 `Athletics`
  다 — "오클랜드"를 붙이면 틀린 사실이 카드에 실린다(실측 2026-09-06: 그 팀은
  새크라멘토 서터 헬스 파크를 쓴다). **모르는 것은 쓰지 않는다.**
- **시즌 누적·통산·상대전적은 판정 입력이 아니다** (2026-09-04 대원칙).
  네가 알고 있는 시즌 성적·통산 기록·과거 맞대결을 근거에 끌어오지 마라.
  판단은 위에 준 최근 표본 안에서만 한다.
  · **타자 대 투수 상대 전적(BvP)을 쓰지 마라.** "이 타자는 저 투수에게 강하다"는
    수십 타석 표본의 통산 기록이고, 위 자료에 들어 있지도 않다. 기억에서
    끌어오면 그것은 **자료가 아니라 네 사전 지식**이다.
  · 팀 간 상대 전적(시즌 상대 성적·최근 맞대결 우위)도 같다. 오늘 나온
    9명과 오늘 던지는 선발이 지난 맞대결의 그 사람들이 아니다.
  · 좌우 **스플릿 성적**(좌투 상대 타율 등)도 시즌 누적이다. 자료11의
    `선발손`은 **오늘의 사실**이라 주는 것이지, 스플릿을 떠올리라는 뜻이 아니다.
  · 선발은 자료4(최근 등판)가 전부다. 최근 등판이 **2경기 이하**면 그 표본은
    그 투수를 대표하지 않는다 — 시즌 라인으로 메우지 말고 **확신도를 낮추고
    0.50 쪽으로 당겨라.** 표본이 적다는 사실 자체가 정보다.
  · 타선은 자료1(3경기 박스스코어)과 자료3(오늘 타순)으로 본다. 3경기 득점은
    표본 3이므로 상대 선발·상대 순위를 감안해 경향으로 읽고 단정하지 마라.
  · 타선 격차와 선발 격차가 반대를 가리키면 **선발 쪽을 무겁게 본다.**
    오늘 던지는 선발 하나가 9명 각각보다 경기에 크게 관여한다.
  · 어느 한쪽 재료가 비어 있으면 **없는 쪽을 나쁘다고 보지 마라.** 수집
    실패이지 그 팀이 약하다는 뜻이 아니다.
- 3경기 표본의 한계를 인정한다. p_home은 0.32~0.68 범위를 벗어나지 않는다.
  자료가 그 이상을 시사해도 0.32 또는 0.68에서 멈춘다.
- 양팀 자료가 팽팽하면 억지로 차이를 만들지 말고 0.50 부근으로 낸다.

- 🔴 **먼저 이 경기가 어떻게 흘러갈지 세워라. 확률은 그 다음이다.**
  숫자를 모아 확률을 만드는 것이 아니라, **이 경기가 어떤 경로로 끝나는지**를
  먼저 그리고 그 경로들의 그럴듯함에서 확률을 낸다.
  · `홈승리경로` — 홈이 이기려면 무엇이 일어나야 하는가. 선발이 몇 이닝을
    끌고, 어느 지점에서 불펜이 들어오고, 그때 상대 타순의 누구를 만나는가.
    **고리마다 자료의 어느 숫자가 그것을 뒷받침하는지 적어라.**
  · `원정승리경로` — 같은 형식.
  · `분기점` — 두 경로가 갈리는 **한 지점**. 다음 회차에 이 문장을 그대로
    조사에 보내므로 **검색 가능한 한 문장**으로 쓴다
    (예: "Wrobleski 가 5이닝을 넘기는가"). 자료가 그 지점을 어느 쪽으로
    기울인다고 말하는지도 함께 적어라.
  · `예상점수` — 그 전개대로 흘렀을 때의 최종 스코어(정수).
    ⚠️ 맞히라는 것이 아니라 **네 전개가 몇 점짜리 경기인지 스스로 확인하라는
       것**이다. 예상 점수와 p_home 이 어긋나면(예: 3점 차인데 p_home 0.52)
       둘 중 하나가 틀린 것이니 다시 보라.
    🔴 **동점을 쓰지 마라.** MLB 는 연장으로 승부를 가르므로 무승부 최종
       스코어가 존재하지 않는다. 박빙이면 1점 차로 쓰고 `우세`·`판단` 에서
       박빙임을 밝혀라.
    🔴 **점수의 승자와 `결론.승자`·`p_home` 방향이 같아야 한다.** 세 곳이
       어긋나면 코드가 예상점수를 **지운다** — 지어낸 점수를 싣지 않기 위해서다.
  ⚠️ 경로는 **자료 안에서만** 세운다. 자료가 말해주지 않는 고리는
     "자료 없음"이라고 적고 넘어가라.

[변수 형식] — 변수는 반드시 이 한 줄 형태로 쓴다.
  "<리스크 서술> — 발생 시 <홈|원정> 방향 약 N%p · 발생 확률 X% · 현재 p에 M%p 기반영 · 근거 <자료 번호>"
🔴 **방향은 반드시 `홈` 또는 `원정` 한국어로 쓴다.** `home`·`away` 로 쓰지 마라 —
   형식 파서가 못 읽으면 그 변수는 예산 검사·조사 루프·원장에서 통째로 빠진다.
🔴 **`발생 확률 X%` 는 자료14 가 그 리스크의 빈도를 알려줬을 때만 쓴다.**
   자료14 의 `같은처지`·`소속팀` 이 "5이닝 이상 35/51(69%)" 처럼 **실제로 몇 번
   중 몇 번이었는지**를 준다 — 그 여집합이 이 리스크의 발생 확률이다
   (69% 가 5이닝을 넘겼다면 조기 강판은 31%).
   ⚠️ 자료14 에 답이 없으면 **이 칸을 통째로 빼라.** 확률을 지어내지 마라.
      근거가 없는 백분율은 정량화가 아니라 장식이다.
   ⚠️ M(기반영)은 대략 `N × 발생 확률` 을 넘지 않는 것이 자연스럽다 —
      거의 안 일어나는 일에 큰 폭을 미리 반영하면 그건 다른 판단이다.
① N·M 은 **자료10 변수 대장의 prior 범위 안에서** 고르고, 인용한 대장 항목
   이름을 근거에 적는다(예: "근거 자료10 이닝분포 p25"). 대장에 없는 크기를
   지어내지 마라. 날씨는 우세가 아니라 **총득점 방향**으로만 읽는다.
   참조가 없으면 "근거 없음 — 보수 반영"이라 쓰되 M 은 0 이 아니어야 한다.
   리스크를 인지하고 확률에 하나도 반영하지 않는 것은 금지다.
② M 의 합은 |p_home − 0.50| 이내여야 한다. 변수 반영이 우세 자체를 만들 수 없다.
③ 변수로 우세를 뒤집지 않는다. p_home 클립(0.32~0.68)은 그대로다.""" + VARIABLE_AXES + """

[출력]
아래 JSON만 출력한다. 다른 텍스트, 마크다운 백틱 금지.
{
  "전개": {"홈승리경로": "고리마다 뒷받침 숫자를 붙여 3~5문장",
          "원정승리경로": "같은 형식",
          "분기점": "두 경로가 갈리는 한 지점. **검색 가능한 한 문장**으로.",
          "예상점수": {"홈": 0, "원정": 0}},
  "결론": {"승자": "팀 이름(자료에 적힌 표기 그대로)",
          "판단": "2~4문장. 전부를 놓고 이 경기를 어떻게 보는지."},
  "p_home": 0.00,
  "우세": "home|away|박빙",
  "근거": ["핵심 근거 정확히 3개, 각각 어느 자료에서 나왔는지 명시"],
  "변수": ["아래 [변수 형식]을 정확히 따른 문자열 1~2개"],
  "직전대비": {"변경입력": ["달라진 입력. 최초 판정이면 빈 배열"],
             "무효화된근거": ["직전 근거 중 더 이상 성립하지 않는 것"],
             "이동": "+N.N%p|-N.N%p|0"},
  "추가확인": ["확신을 세우는 데 필요한 미확인 정보 0~2개"],
  "뉴스반영": {"적용": true|false, "조정폭": "+N.N%p|-N.N%p|0", "사유": "1문장"},
  "상황판정": {"결론": "지지|불일치|무관", "사유": "1~2문장", "출처": "계정 또는 도메인"},
  "확신도": "상|중|하"
}
"""


#: 🔒 [BAT-6] 자료3 타자 문구. **동결 게이트가 꺼져 있으면 빈 문자열**이라
#   프롬프트 바이트가 BAT-4 이전과 같다. 문구는 여기에만 있다(사본 금지).
_BATTER_NOTE = """
   각 타자에 `최근5`(그 선수의 **최근 5경기 합계**)가 붙어 있으면
   **순서보다 그 숫자를 먼저 본다.**
   ⚠️ **거기 있는 칸만 사실이다.** 원본 박스스코어에 없는 항목은 우리도
      만들지 않는다 — 없는 칸을 결손으로도 0으로도 읽지 마라.
   ⚠️ `최근5` 가 **없는 선수는 자료가 없는 것이지 부진한 것이 아니다.**
   ⚠️ `경기` 가 2 이하면 그 숫자는 그 타자를 대표하지 않는다 — 표본이 얇다는
      사실 자체가 정보다. 타율·OPS 는 주지 않는다. 필요하면 네가 나눠 읽되
      **시즌 성적을 기억에서 끌어오지 마라.**"""


def baseball_material_note() -> str:
    """자료3 타자 문구. 동결 게이트가 꺼져 있으면 **빈 문자열**."""
    from app.config import get_settings

    return _BATTER_NOTE if get_settings().batter_material_enabled else ""


def fill(template: str, **fields: str) -> str:
    """{{KEY}} 만 치환한다. 출력 JSON 예시의 중괄호는 건드리지 않는다."""
    out = template
    for key, value in fields.items():
        out = out.replace("{{" + key + "}}", value)
    return out


# ═══════════════════════════════════════════════════════════════════
# [ORD-1 2026-09-11 사용자 지시] **순서만 바꾼다.**
#   "경기가 나왔어 → AI 가 먼저 변수 및 갈림길을 찾는다 → 그 후에 인공위성·
#    퍼플렉시티·X 가 보강자료를 찾는다 → 결론을 낸다 → 애매한 것은 우리 DB 를
#    참조한다."
#  종전(SCT-1): 자료1~14 를 **먼저** 통째로 주고 그 위에서 변수를 만들었다.
#    실측 2026-09-11 MLB 15경기 — 근거 45줄 중 DB 인용 40줄, 오늘의 사실 3줄.
#    DB 가 먼저 오면 DB 가 답을 정한다.
#  이제: 경기 정보만 보고 갈림길을 세우고 → 그것만 조사하고 → 결론 → DB 는 마지막.
# ═══════════════════════════════════════════════════════════════════

#: ① 갈림길·변수·조사요청. **DB 수치를 보지 않는다.**
#  ⚠️ 채점 규칙은 `VARIABLE_GRADEABLE` 을 그대로 쓴다 — 여기 다시 적으면 사본이고,
#     임계 없는 변수가 변수 대장을 망가뜨린다.
PRESCOUT = """당신은 야구 경기 **선임 분석관**이다. 지금 네 앞에 통계표는 없다.
경기 한 장만 보고 **이 경기가 어디서 갈리는가**를 세우는 것이 네 일이다.

[경기] {league} · {away} (원정) @ {home} (홈)
[오늘] {today} (KST)
[경기 정보]
{brief}

네 일은 셋이다.
① **갈림길** — 오늘 승부가 갈릴 지점 1~3개. 각각 **예/아니오로 답이 나오는
   한 문장**이어야 한다 (예: "이마나가가 6이닝을 넘기는가").
② **변수** — 그 갈림길이 나쁜 쪽으로 터졌을 때의 리스크. **리스크 서술만
   쓴다** — 방향(홈/원정)과 %p 는 적지 마라. 그것은 결론 단계의 몫이다.
③ **조사요청** — 그 갈림길을 **오늘 흔들 수 있는 사건**을 찾아오게 한다.
   위성·퍼플렉시티·X 가 이 문장을 그대로 들고 검색하러 간다.

🔴 **승패·확률을 적지 마라.** p_home 도 우세도 네 몫이 아니다. 결론은 다음
   단계에서 낸다. 너는 **무엇이 이 경기를 가르는가**만 말한다.
🔴 **수치를 지어내지 마라.** 방어율·승률·최근 성적을 기억으로 적지 마라 —
   너에게 그 자료는 주어지지 않았고, 기억은 자료가 아니다.

🔴🔴 **조사요청에 성적을 묻지 마라.** 시즌 성적·평균자책점·팀 타율·OPS·
   게임로그·통산 전적·좌우 스플릿은 **어느 날에나 같은 값**이고 오늘을
   설명하지 않는다. 그건 이미 아는 것을 다시 사 오는 일이다.
   > 실측 2026-09-12: 실제로 나간 조사요청 38문 중 **28문(74%)이 성적 조회**
   > 였다. 적중률은 성적 65% · 변수 67% 로 비슷했다 — 검색이 못 찾는 것이
   > 아니라 **우리가 변수를 안 물었다.**

   ❌ 이렇게 묻지 마라 (실제로 나갔던 질문이다)
       "George Kirby 2026 season stats against Athletics"
       "Arizona Diamondbacks bullpen ERA last 14 days September 2026"
       "밀워키 브루어스 2026 시즌 좌완 선발 투수 상대 팀 OPS"

   ⭕ 이렇게 물어라 — **오늘/이번 주에 바뀐 것**을 묻는다
       "Wilber Dotel 선발 등판 경험과 9월 콜업 경위, 투구수 제한 여부"
       "Cade Cavalli 최근 결장 사유와 복귀 후 투구수 제한"
       "Max Scherzer 등 부상 이력과 최근 등판에서의 몸 상태 언급"
   (셋 다 2026-09-11 스모크에서 **실제로 답이 온 질문**이다)

🔴 **무엇을 묻는가 — 오늘 바뀐 사건만 묻는다.** 아래를 훑고 이 경기에서
   실제로 큰 것만 2~5개 골라라.
     · 부상·통증 — IL 등재/복귀, 타구 맞음, 통증 호소, 검진 결과
     · 등록 — KBO **1군 등록·말소**, MLB **IL·옵션·콜업**, 트레이드
     · 선발 — 예고 변경, 등판 간격 단축, **투구수 제한**, 개막 후 첫 선발
     · 불펜 — **연투**, 어제 몇 구, 마무리 가용 여부, 소진 보도
     · 라인업 — 주전 결장·휴식 예고, 타순 변경 예고
     · 환경 — 돔 **지붕 개폐**, 바람·비, 구장 변경
     · 팀 사정 — 감독 코멘트, 순위 경쟁·포기, 은퇴식·특별 경기
🔴 **조사요청은 검색해서 답이 나오는 문장**이어야 한다. "팀이 잘하는가" 같은
   문장은 검색되지 않는다.

🔴🔴 **질문마다 "어느 층에서 찾을지"를 정하고, 그 층에 맞게 써라**(`조사요청_층`).
   사실마다 공시되는 단위가 다르다. 층을 틀리면 **구조적으로 못 찾는다.**
   > 실측 2026-09-12 (KBO 두산 vs NC, 같은 사실을 층만 바꿔 물었다):
   >   "엔트리 등록·말소" — 경기 0건 · 팀 0건 · **리그 1건** · 계정 0건
   >   리그 층이 찾아온 문장: "9월11일 등/말소 현황: 등록 롯데 박찬형…
   >   말소 KIA 박상준…" — **그 문장에 두산도 NC도 없다.**
   >   경기명으로 좁히면 이 게시물은 영원히 안 잡힌다.

     · `리그` — **리그 일괄 공시**로 나오는 것. 엔트리 등록·말소, 트랜잭션,
       IL 등재/복귀, 내일의 선발투수 공시.
       ⚠️ 이 층의 질문에는 **팀 이름을 넣지 마라.** 공시 문장에 우리 팀이
          안 나올 수 있다. 리그명 + 날짜 + 사건으로만 써라.
     · `팀` — 한 팀 안에서만 도는 것. 라인업 결장·휴식, 감독 코멘트.
     · `선수` — 한 사람에게 붙는 것. 부상·통증·투구수 제한·등판 간격.
       ⚠️ 이 층은 **선수 이름을 반드시** 넣어라.
     · `구장` — 지붕 개폐·바람·비.
     · `경기` — 위 어디에도 안 맞는 이 경기 고유의 것.

⚠️ 층과 질문의 **개수·순서를 맞춰라.** 어긋나면 층을 통째로 버리고 종전처럼
   경기 단위로 찾는다 — 잘못 짝지어 엉뚱한 층으로 묻는 것보다 낫다.
🔴 **조사요청은 리그를 불문하고 영어로 쓴다.**
   > 실측 2026-09-12, 같은 질문 4개를 3회씩:
   >   X(그록)  한국어 [0,0,0] (0/12) · 영어 [0,2,2] (3회 누적 3/4)
   >   퍼플렉시티 한국어 [4,4,1] · 영어 [4,4,4]
   > 한국어로 물으면 **X 채널이 통째로 죽는다.**
🔴 **KBO·NPB 는 같은 질문을 그 나라 말로도 한 벌 더 써라**(`조사요청_현지어`).
   영어로 먼저 찾고, 못 찾은 것만 그 말로 다시 찾는다. KBO 는 한국어,
   NPB 는 일본어. **같은 질문의 번역이어야 한다** — 새 질문을 만들지 마라.
   MLB 는 이 칸을 빈 배열로 둔다(영어가 곧 현지어다).
⚠️ 그렇다고 억지로 개수를 채우지 마라. 오늘 이 경기에 걸린 사건이 둘뿐이면
   둘만 물어라 — 지어낸 질문은 "찾지 못함" 한 줄로 돌아올 뿐이다.
""" + VARIABLE_GRADEABLE + """

[출력] 아래 JSON만 출력한다. 다른 텍스트, 마크다운 백틱 금지.
{{
  "갈림길": [{{"질문": "예/아니오 한 문장", "왜": "왜 여기서 갈리는지 1문장"}}],
  "변수": ["채점 가능한 리스크 서술 1~3개. 방향·%p 는 적지 마라"],
  "조사요청": ["영어로 쓴 검색 가능한 한 문장 2~6개"],
  "조사요청_현지어": ["위 질문을 그 리그의 말로 옮긴 것. 순서·개수를 맞춘다. MLB 는 []"],
  "조사요청_층": ["리그|팀|선수|구장|경기 중 하나씩. 조사요청과 순서·개수를 맞춘다"]
}}"""

#: ② 보강 — 퍼플렉시티·X 에게 **①이 정한 질문만** 던진다.
#  ⚠️ 일반 검색이 아니다. 질문 목록이 곧 검색 범위다.
REINFORCE_ASK = """오늘({today}) {league} 경기 "{away} @ {home}" 를 앞두고
아래 질문들의 **답만** 찾아라. 질문에 없는 것은 가져오지 마라.

[질문]
{questions}

🔴 **답을 모르면 그 항목을 빼라.** 추측·일반론·시즌 통계로 채우지 마라.
   "찾지 못했다"가 지어낸 답보다 낫다.
🔴 **경기 전 사실만.** 끝난 경기의 결과·하이라이트·팬 반응은 빼라.
🔴 `url` 은 실제 주소여야 한다. 없으면 그 항목을 빼라.

🔴 **언제 있었던 일인지를 `시점`(YYYY-MM-DD)에 적어라.**
   > 실측 2026-09-12: 변수를 묻기 시작하자 **4월·6월·7월 기사**가 오늘 일처럼
   >   돌아왔다 — "Scherzer 4월 27일 부상자 명단", "May 7월 초 발목 타박상".
   >   지난 일을 오늘 상태로 읽으면 그건 조사가 아니라 오해다.
   ⚠️ **날짜를 모른다고 답을 버리지는 마라.** `시점` 을 빈 칸으로 두고 답은
      남겨라 — 날짜 없는 사실이 사실 없는 것보다 낫다.
      (규칙을 조이면 답이 함께 줄어든다. 실측 2026-09-12: 같은 질문 2개에
       PPLX 가 6회 중 2~6건으로 흔들렸고 한 번은 `{{"답": []}}` 을 냈다 —
       표본이 작을 때 "규칙 탓"과 "채널 변동"은 구분되지 않는다.)
⚠️ 같은 선수에 대해 여러 시점이 있으면 **가장 최근 것 하나만** 남겨라.
   이력 나열은 조사가 아니다. 오래된 일인데 오늘도 유효하면 그 사실을
   답 안에 밝혀라 ("아직 부상자 명단", "제한 유지").

[출력] 아래 JSON만 출력한다. 다른 말 금지. 최대 8건.
{{"답": [{{"질문번호": 1, "답": "사실 1문장", "시점": "YYYY-MM-DD (모르면 \"\")",
          "소스유형": "공식|기록|뉴스", "url": "..."}}]}}
🔴 **객체로 감싼다.** 배열만 내보내면 우리 파서가 첫 항목만 읽고 나머지를 버린다
   (실측 2026-09-11: 정답 2건 중 1건만 남았다).
찾은 것이 없으면 `{{"답": []}}` 을 출력하라."""

#: ③ 결론. 🔴 [ORD-3 2026-09-11 사용자 지시] **어느 팀이 이기는지만 판다.**
#    "추천 로직도 다 삭제...수치는 전부다 삭제..." / "설명도 삭제...서치에 의한
#     정보만 명시..." / "그리고 어느팀이 승리한다만 제미나이가 판다..."
#  그래서 `MATCHUP_OUTPUT_BLOCK` 을 쓰지 않는다 — 확률·근거·변수·전개·확신도는
#  전부 설명이고, 설명은 이제 카드에 나가지 않는다. 출력은 팀 이름 하나다.
#  ⚠️ `.replace()` 로 채운다(`MATCHUP` 과 같은 규약).
CONCLUDE = """당신은 야구 판정가다. **어느 팀이 이기는지만 답한다.**

[경기] {{LEAGUE}} · {{AWAY}} (원정) @ {{HOME}} (홈)
[오늘] {{TODAY}} (KST)
[경기 정보]
{{BRIEF}}

[갈림길] — 분석관이 경기만 보고 세운 것이다:
{{BRANCHES}}

[조사 결과] — 위성·퍼플렉시티·X 가 **위 갈림길만 겨냥해** 오늘 찾아온 것이다:
{{EVIDENCE}}

🔴 **여기 있는 것이 전부다.** 우리 DB(최근 3경기 박스스코어·팀 폼·레이팅·
   불펜·타순)는 너에게 오지 않는다.
🔴 **기억으로 판단하지 마라.** 위 [조사 결과] 에 없는 성적·순위·상대전적은
   너에게 주어지지 않았다. 조사 결과가 가리키는 쪽을 고른다.
🔴 **확률·근거·설명을 적지 마라.** 우세도 확신도도 묻지 않는다. 승자 하나다.
🔴 **승자는 위 두 팀 중 하나의 이름을 그대로 쓴다.** `home`·`away` 로 쓰거나
   경기에 없는 팀을 쓰면 그 판정은 버려진다(실측 2026-09-11: 경기에 없는
   `KT Wiz` 가 4회 중 1회 나왔다).

[출력] 아래 JSON만 출력한다. 다른 텍스트, 마크다운 백틱 금지.
{"승자": "{{AWAY}} 또는 {{HOME}} 중 하나를 그대로 적는다"}
"""


# ═══════════════════════════════════════════════════════════════════
# [ORD-11 · 2단계] 사용자 지시 2026-09-12: "그후 ai에게 정보를 준다.ai가 거른다"
# ═══════════════════════════════════════════════════════════════════

#: ② 선별. 질문 없이 긁어 온 것을 **AI 가 거르고**, 거기서 갈림길·변수를 뽑는다.
#  🔴 채택·기각을 **번호로** 받는다. 문장을 다시 쓰게 하면 그것이 창작이고,
#     원문이 바뀌어 카드·감사가 대조할 수 없게 된다.
TRIAGE = """당신은 야구 경기 **선임 분석관**이다. 아래는 오늘 이 경기 주변에서
**질문 없이 긁어 온 것**이다 — 쓸모없는 것이 섞여 있다.

[경기] {league} · {away} (원정) @ {home} (홈)
[오늘] {today} (KST)
[경기 정보]
{brief}

[수집물] 번호 · [출처] · 내용
{rows}

네 일은 넷이다.

① **거른다.** 이 경기의 승부와 닿지 않는 것을 기각하라.
   기각할 것: 지난 경기 결과·하이라이트, 다른 팀 이야기, 티켓·중계·좌석 안내,
   구장 소개·관광, 시즌 통계 나열, 같은 사실의 중복.
   ⚠️ **의심스러우면 채택하라.** 거름이 과하면 판정이 굶는다. 확실히 무관한
      것만 기각한다.

② **갈림길을 세운다.** 채택한 것에 근거해, 오늘 승부가 갈릴 지점 1~3개.
   각각 **예/아니오로 답이 나오는 한 문장**이어야 한다.
   🔴 **채택한 것에 없는 사실로 갈림길을 만들지 마라.** 기억으로 지어낸
      갈림길은 조사되지 않은 갈림길이고, 그건 그냥 네 짐작이다.

③ **변수를 정한다.** 그 갈림길이 나쁜 쪽으로 터졌을 때의 리스크.
   **리스크 서술만 쓴다** — 방향(홈/원정)·%p 는 적지 마라. 결론 단계의 몫이다.

④ **없는 것을 적는다.** 갈림길을 확정하려면 필요한데 수집물에 **없는** 것.
   그중 우리 DB 가 줄 수 있는 것은 `DB요청` 에도 적어라.
   우리 DB 에 있는 것: 최근 3경기 박스스코어 · 오늘 타순 · 선발 최근 등판 ·
   불펜 최근 폼과 가용성 · 실력 레이팅 · 일정/이동/날씨.
   ⚠️ **습관적으로 채우지 마라.** 채택한 것으로 갈림길이 서면 빈 배열이다.

🔴 **번호만 쓴다.** 채택·기각은 위 수집물의 번호로만 답하라. 내용을 옮겨
   적거나 고쳐 쓰지 마라 — 원문은 우리가 갖고 있다.
🔴 **모든 번호는 정확히 한 번씩** 채택이나 기각에 들어가야 한다. 빠뜨리지 마라.
🔴 **기각에는 사유를 한 줄 붙여라.** 사유 없는 기각은 셀 수 없고, 세지 못하면
   "거름이 과한가"를 다음에 물을 수 없다.
""" + VARIABLE_GRADEABLE + """

[출력] 아래 JSON만 출력한다. 다른 텍스트, 마크다운 백틱 금지.
{{
  "채택": [1, 4, 7],
  "기각": [{{"번호": 2, "사유": "어제 경기 결과"}}],
  "갈림길": [{{"질문": "예/아니오 한 문장", "왜": "근거가 된 채택 번호와 이유"}}],
  "변수": ["채점 가능한 리스크 서술 1~3개. 방향·%p 는 적지 마라"],
  "없는것": ["갈림길 확정에 필요한데 수집물에 없는 것 0~3개"],
  "DB요청": ["위 '없는것' 중 우리 DB 가 줄 수 있는 것. 없으면 []"]
}}"""


#: ③-a 판정. **조사 결과만으로 승자를 낸다. DB 는 4단계에서 따로 온다.**
#  🔴 [ORD-12 사용자 지시] "나로 해라..확신 한 칸만 살려라."
#     ORD-3 에서 지운 것 중 **`확신` 하나만** 되살린다. 그건 설명이 아니라
#     라벨이고, 4단계에서 DB 와 모순이 났을 때 낮출 곳이 필요하다.
#  ⚠️ 확신 눈금은 `MATCHUP` 의 `확신도` 와 **같은 세 값**을 쓴다(상|중|하).
#     새 눈금을 만들지 않는다.
JUDGE2 = """당신은 야구 판정가다. **어느 팀이 이기는지만 답한다.**

[경기] {{LEAGUE}} · {{AWAY}} (원정) @ {{HOME}} (홈)
[오늘] {{TODAY}} (KST)
[경기 정보]
{{BRIEF}}

[갈림길] — 분석관이 오늘 수집물을 보고 세운 것이다:
{{BRANCHES}}

[채택 자료] — 분석관이 **거르고 남긴 것**이다. 판단의 근거는 이것뿐이다:
{{KEPT}}

[확인하지 못한 것] — 오늘 찾지 못했다:
{{MISSING}}

🔴 **여기 있는 것이 전부다.** 우리 DB(최근 3경기·팀 폼·레이팅·불펜·타순)는
   이 자리에 오지 않는다.
🔴 **기억으로 판단하지 마라.** 위 [채택 자료] 에 없는 성적·순위·상대전적은
   너에게 주어지지 않았다. 채택 자료가 가리키는 쪽을 고른다.
🔴 **확률·근거·설명을 적지 마라.** 승자와 확신, 둘뿐이다.
🔴 **승자는 위 두 팀 중 하나의 이름을 그대로 쓴다.** `home`·`away` 로 쓰거나
   경기에 없는 팀을 쓰면 그 판정은 버려진다(실측 2026-09-11: 경기에 없는
   `KT Wiz` 가 4회 중 1회 나왔다).

[확신] 셋 중 하나만 쓴다.
  · `상` — 채택 자료가 갈림길에 **직접** 답했고, 방향이 한쪽으로 모인다.
  · `중` — 일부만 답했거나 자료가 서로 다른 쪽을 가리킨다.
  · `하` — **[확인하지 못한 것] 이 갈림길의 핵심**이거나, 채택 자료가 얇다.
🔴 **모르면 `하` 다.** 확신을 높게 쓰는 것이 이 구조에서 가장 나쁜 실패다 —
   우리는 확률을 내지 않으므로, 확신이 유일한 "얼마나 믿는가" 신호다.

[출력] 아래 JSON만 출력한다. 다른 텍스트, 마크다운 백틱 금지.
{"승자": "{{AWAY}} 또는 {{HOME}} 중 하나를 그대로 적는다", "확신": "상|중|하"}
"""


#: ④ DB 참조. 🔴 [ORD-13 사용자 지시] "db가치를 내린다..db는 단순 참조용이다."
#  DB 는 **근거가 될 수 없다.** 확인하거나 반박할 수만 있고, 승자를 뒤집지 못한다.
#  근거: ORD-1 실측 — DB 를 뒤에 붙였더니 ④가 3/3 돌았고 NYM@NYY 는 승자째
#        뒤집혔다(0.54 NYY → 0.46 NYM). 그때는 막을 장치가 없었다.
DB_CHECK = """당신은 방금 이 경기의 승자를 정했다. 이제 **우리 기록**을 보여준다.

[경기] {{LEAGUE}} · {{AWAY}} (원정) @ {{HOME}} (홈)
[네 판정] 승자 {{WINNER}} · 확신 {{CONF}}
[네가 본 갈림길]
{{BRANCHES}}

[참조 · 우리 기록] — 네가 `DB요청` 으로 부른 것이다:
{{DBROWS}}

🔴 **우리 기록은 근거가 될 수 없다. 참조다.** 판정은 이미 조사 결과로 내렸다.
   기록은 그것을 **확인하거나 반박할 수만** 있다.
🔴 **승자를 뒤집지 마라.** 기록이 다른 쪽을 가리켜도 승자는 그대로 둔다.
   네가 할 일은 그 어긋남을 **알리는 것**이지 다시 정하는 것이 아니다.
🔴 **새 근거를 만들지 마라.** 이 기록으로 판정을 설명하려 들지 마라 —
   설명은 카드에 나가지 않는다.

[판정] 둘 중 하나만 쓴다.
  · `확인` — 우리 기록이 네 판정과 **같은 쪽**이거나, 어긋나지 않는다.
  · `모순` — 우리 기록이 **갈림길의 핵심**에서 네 판정과 반대를 가리킨다.
🔴 **`모순` 은 아껴 써라.** "조금 다르다"는 확인이다. 갈림길을 뒤집을 만한
   것일 때만 모순이다. 모순이면 확신이 한 단계 내려간다.
🔴 **모순에는 사유를 한 줄 붙여라.** 어느 기록이 어느 갈림길과 어떻게
   어긋나는지. 사유 없는 모순은 셀 수 없다.

[출력] 아래 JSON만 출력한다. 다른 텍스트, 마크다운 백틱 금지.
{"판정": "확인|모순", "사유": "모순일 때만 한 줄. 확인이면 빈 문자열"}
"""
