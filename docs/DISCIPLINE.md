# AnalystBot 운용 규율

> 이 문서의 모든 규칙은 **실제 실패에서 도출**됐다. 각 항목에 "왜"를 함께 적는다.
> 규율과 충돌하는 요청을 받으면 **실행하기 전에 그 사실을 먼저 보고**한다.
> **목표는 적중**이다. 필요 없다고 지시받은 기능은 남기지 않는다.

**코드 반영 여부 표기**: ✅ 코드로 강제됨 · ⚠️ 부분 반영 · ❌ 미구현(사람이 지켜야 함)

---

## 1. 픽 선정 규율

> **2026-08-29**: 야구와 축구를 나눈다. 이번 개편은 **야구만**이다. 축구(스켈람,
> 더블찬스·핸디·언오버, Odds, 2-소스)는 손대지 않는다. 한 번에 두 종목을 갈아엎으면
> 검증이 불가능하다. 축구 개편은 야구가 안정된 뒤 별도 작업으로 한다.

### 1-A. 야구 (KBO · NPB · MLB)

#### 1-A-1. 입력 — 폼·라인업·선발·72시간 뉴스 ✅ 명문화 (2026-08-29)

판정 입력은 다음만 쓴다.

- 양 팀 **지난 3경기 박스스코어**
- **오늘 타순**(확정된 9명)과 **오늘 선발**의 최근 등판
- 팀당 **72시간 뉴스**(공식·포털. 커뮤니티 금지)

**금지**: ERA, xwOBA 등 **성적 지표의 시즌 누적치**를 판정 입력으로 쓰는 것.
**허용**: 지난 3경기 **각 상대팀의 현재 순위·승률 1줄** — 상대 수준 맥락 전용.
시즌 승률을 매치업 프롬프트의 팀 실력 단언으로 쓰지 않는다.

야구 **승부는 배당을 조회하지 않는다.** 배당 가격·암시확률이 승률·추천 자격에
들어가지 않는다. 야구 마켓은 **승패(머니라인)만**. 런라인·F5·언더오버는
추천·보드에서 쓰지 않는다(이관 중이면 보드만 남기고 추천에서 뺀다).

> **왜**: 시즌 누적 지표는 "그 팀의 이름값"이 되고, 오늘 9명·최근 3경기의 실제
> 내용과 섞인다. 상대 순위·승률 1줄은 "지난 3경기가 누구를 상대로였는지"만 말한다.

#### 1-A-2. 추천 게이트 = 2조건 + 거부권 2개 ✅ `qualifies()` · `_approve`

**게이트 (둘 다 필요)**

1. 승률 ≥ `MIN_WIN_PROB`(0.58). 원정 픽은 `away_prob_penalty` +5%p → **63%**.
   새 모델은 홈 이점을 3경기에서 제대로 못 볼 수 있어 보수적 가드로 남긴다.
2. 라인업 확정(`pick_state == final`). 예상 타순은 최종 추천이 아니다.

**거부권 (게이트 통과 후에 박탈 — 게이트 조건이 아님)**

- 판정 확신도 **'하'** → 추천 박탈
- 판정 **'패스 권장'** → 추천 박탈

3경기 표본은 과신하기 쉽다. 이 두 거부권은 안전장치다. '보통'은 야구에서
박탈 사유가 아니다.

**폐기 (야구)**

- data·model **2-소스** 조건 — 쓰지 않는다.
- 수익률 +20% / 승률–배당환산 25%p — 배당을 안 보므로 성립 불가. 삭제.

**표시 전용 (추천과 무관)**

- 신호등 🟢 승률 62%↑ / 🟡 58~62% / 🔴 미달 / ⚪ 미산출.
  `signal_green_prob = 0.62`는 **표시 레이어**다. 추천 임계가 아니다.

**NPB last-3**: Yahoo 스포츠나비 박스스코어 3경기 수집이 라이브로 검증됐다
(2026-08-29, 12팀 36/36 상대 순위·승률 부착, 타격표 대조, 필드 누락 0).
`npb_last3_verified=True` — KBO·MLB와 같은 추천 게이트.

정리: **야구 추천 = 58%(원정 63%) + 라인업 확정, 단 확신도 '하'/패스면 박탈.**

#### 1-A-3. 오늘 9명 ✅ `today_nine`

야구 승부 단위는 **오늘 선발 타순 9명**이다. 평소 주전 결장은 그 9명이 누구인지를
설명하는 주석이다. 결장 건수로 홈/원정 사이드를 뒤집지 않는다.

> **왜**: 파드리스전에서 이탈을 서술에만 썼고 승률은 그대로였다. 반대 사고
> (2026-08-28 NPB 세이부)는 주전 4명 결장 건수로 사이드를 뒤집었다.
> 빠진 사람 수는 사실이지만 오늘 방망이는 그 9명이다.

#### 1-A-4. 라인업 확정 전 픽은 '예비'다 ✅ `app/collectors/lineups.py`

- **예상(predicted)을 확정(confirmed)으로 취급 금지.** 타순 9명이 채워져야 확정이다.
- 픽 상태 2단계: `🕐 잠정 — 라인업 확정 전` / `✅ 최종 — 라인업 확정 반영`
- StatsAPI와 research의 선발이 다르면 **StatsAPI 우선**, 양쪽 모두 표시하고 `conflict`.
  **불일치 상태는 예비 픽만 허용하고 최종 픽 자격이 없다.**
- 확정 수신 시 해당 경기만 재판정하고, 픽이 뒤집히면 명시한다.
- **NPB 크롤·분석은 시작 15분 전(18:00 → 17:45)에 끝낸다.** 그 시각 이후 NPB
  재판정은 하지 않는다.

> **왜**: 파드리스전에서 stats와 research의 선발이 달랐는데 최종 픽처럼 나갔다.

---

### 1-B. 축구 (현행 유지 — 2026-08-29 야구 개편에서 손대지 않음)

#### 1-B-1. 2-소스 룰 ✅ `app/engine/markets.py::_approve`

추천 픽은 독립 근거 축 **2개 이상**의 지지가 필요하다. 축은 ①올 시즌 실데이터
②검증된 전문가 픽 ③모델. **모델 단독 픽은 추천 금지.**
(2026-08-25: ④시장 방향 축은 시장 배제 전환으로 제거됐다.)

> **왜**: 8/22 빌라 3.30, 8/23 AGF 1.79 — 둘 다 모델만 우세를 봤고, 둘 다 실패했다.

#### 1-B-2. ~~괴리 검증 룰~~ — **2026-08-25 폐기** ❌

시장 확률을 기준점으로 쓰는 규칙이라 시장을 판정에서 빼면 성립하지 않는다.
남는 위험은 결장·라인업 수집으로 대체 방어한다.

#### 1-B-3. 승률 판정

`p_final = 0.50*p_model + 0.50*p_claude`. **시장 가중치 0.** 축구는 스켈람 분포에서
승패·더블찬스·핸디·언오버가 나온다. Odds는 축구만 조회한다.

#### 1-B-4. 추천 자격 ✅ `qualifies()`

`승률 ≥ MIN_WIN_PROB(0.58)`. 원정 픽은 +5%p(63%). 배당 하한은 쓰지 않는다.
축구는 킥오프 직전 라인업이라 야구의 라인업 `final` 게이트를 쓰지 않는다.

#### 1-B-5. 신뢰도 연동 ⚠️ 부분 반영

- ✅ 판정 신뢰도 **'낮음'** → 전 마켓 추천 자격 박탈
- ✅ 판정이 '패스 권장' → 전 마켓 제외
- ✅ **축구 승패 단식**은 신뢰도 **'높음'**에서만 허용

#### 1-B-6. 시즌 초 가드 ⚠️ 부분 반영

시즌 초(라운드 5 미만)에는 모델 가중을 줄인다. **모델은 작년 팀을 보고 있을 수 있다.**

- ✅ 데이터가 얇은 리그는 시장 수축 λ를 0.6으로 키운다 (`shrink_lambda`)
- ✅ 올 시즌 실데이터 0건 경기는 신뢰도 자동 '낮음' + 추천 박탈
- ❌ **'라운드 N < 5' 조건 자체는 구현돼 있지 않다.**

> **왜**: 빌라는 여름에 주전 5명이 빠져나갔는데 모델은 지난 시즌 H2H로 계산하고 있었다.

#### 1-B-7. 승패 단식 편중 금지 ✅ `build_candidates` + 마켓 보드 ⑧

경기당 승패·더블찬스·핸디캡·언더오버를 **동일 기준으로 평가**해 최적 마켓을 고른다.
조합도 전 마켓 승인 풀에서 구성하며, 같은 경기의 상관 레그 2개는 한 조합에 넣지 않는다.

> **왜**: 8/23 AGF 승은 2-2로 실패했지만 **더블찬스는 적중**이었다.

## 2. 데이터 신뢰 규율

### 2-1. LLM 응답은 200 OK여도 데이터가 아닐 수 있다 ✅ `app/research/validate.py`
**validate 통과 전 사용 금지.** 상세는 [RESEARCH_VALIDATION.md](RESEARCH_VALIDATION.md).

> **왜**: Perplexity가 데이터를 못 찾자 200 OK로 "왜 못 찾았는지" 설명하는 산문을 JSON
> 필드에 담아 반환했고, 그것이 `선발 최근:` 자리에 데이터인 척 출력됐다.

### 2-2. 재료 없으면 분석 생성 금지 ✅ `render_game_section`
**야구**: 지난 3경기 폼이 없으면 분석을 만들지 않고 수집 실패로 쓴다.
**축구**: recent_form·전문가 픽·결장 정보가 모두 비면 심층 분석을 만들지 않고
**"최신 데이터 수집에 실패해 분석할 수 없습니다"**라고 정직하게 쓴다.

> **왜**: 재료 0인 상태에서도 시장 배당만으로 그럴듯한 분석이 생성됐다.

### 2-3. LLM 수치는 API와 교차검증, 충돌 시 API가 이긴다 ✅ 규칙 / ⚠️ 감시
딥서치·Claude가 말한 배당·스탯이 API 값과 다르면 **API 값을 쓴다**.
- ✅ 야구 승부 배당·언더오버 라인을 수집하지 않는다
- ⚠️ `app/research/crosscheck.py` — **MLB만** 대상(축구는 대조 가능한 무료 소스가 얇다)
- 야구 판정 입력에 시즌 ERA·xwOBA를 넣지 않는다 (1-A-1)

### 2-4. 이상치는 가치가 아니라 오류 신호 — **축구만** ✅ `_approve`
**야구**: 배당을 조회하지 않으므로 수익률 +20% / 승률–배당환산 25%p 플래그는
**폐기**한다 (2026-08-29).

**축구**: 배당 숫자가 깨진 경우만 플래그한다. 판정 기준이 아니라 "이 배당이
이상하다"는 데이터 검증 장치다.

> **왜**: 3-way 배당을 2-way로 디빅해 전 경기가 +EV로 보였던 사고가 있었다.

---

## 3. 자금 규율 (사용자 준수 사항)

> 이 절은 코드가 강제할 수 없다. **봇은 권장액만 제시하고 집행하지 않는다.**

- **플랫 스테이크**: 전 픽 동일액(자금 1%). **연패 후 증액(복구 베팅) 금지** —
  이 규칙이 깨지는 순간 시스템 전체가 무의미해진다. 나머지 규율을 다 지켜도 소용없다.
- **하루 최대 2픽**, 조합은 단식 권장액의 **절반 이하**.
- **주간 -8% 도달 시 그 주 중단.**
- **감정 상태에서 베팅 금지.** 화가 난 상태는 멈추라는 신호다.
- ✅ **자동 베팅 집행 기능은 어떤 형태로도 구현하지 않는다.** (절대 규칙)
- ✅ **분석 카드 자동 발송**은 KBO·NPB·MLB, 관리자 채팅만 허용한다.
  **NPB 크롤·분석은 시작 15분 전(18:00 → 17:45)에 끝낸다.** 그 시각 이후 NPB
  재판정은 하지 않는다 (`NPBStopBefore`·`NPB_FINISH_MIN`). 타순이 뜨면 그 경기만
  바로 판정하고 끝나는 대로 보낸다. 선발·타순이 바뀌면 다시 보낸다
  (`asia_pregame_5m`, 2026-08-28 사용자 지시). MLB는 사이트가 statsapi라 Go HTML
  크롤러가 없고, 아침 05–11 KST 5분 폴링(`mlb_pregame_5m`) + 슬레이트 캐시
  (`analysis:mlb:{mlb_slate_date}`)로 같은 회로를 탄다 (2026-08-29 사용자 지시).
  캐시·판정이 없으면 빈 카드를 보내지 않는다. **축구는 요청할 때만.**

---

## 4. 목표 — 적중

**목적은 이 경기에서 이길 팀을 맞히는 것이다.** CLV·장기 EV·방식 비교 성적표로
시스템을 운영하지 않는다. 픽 채점 회로(`grader`)는 2026-08-29 사용자 지시로 삭제했다.

카드·판정·추천은 전부 **오늘 경기 적중**을 위해 존재한다. 필요 없다고 지시받은
기능은 남기지 않는다.

---

## 5. 개발 규율

- **코드 탐색·수정은 Serena 우선** (`find_symbol` → `replace_symbol_body`).
  **파일 전체 재작성 금지** — collectors/research/engine 간 의존이 많아 참조 확인은
  `find_referencing_symbols`로 한다.
- **새 필터·가드를 추가할 때는 반대 위험(정상 데이터 폐기)을 함께 측정한다.**
  > **왜**: 미확보 산문 차단 키워드를 넓혔더니 실수치가 섞인 문장이 통째로 버려졌다
  > (Reds 불펜 "최근 7일 24.2이닝" 손실). 한 방향만 보면 반대편에서 새 오류가 난다.
- **필요 없다고 지시받은 기능은 남기지 않는다.** 설명하고 설득하는 단계가 아니다.
  채점기·성적표가 그 예다 (2026-08-29).
### 5-1. 지시받지 않은 파라미터·설정 변경 금지 ❌ (사람이 지켜야 함)

`max_tokens`, 배치 크기, 타임아웃, 모델명, 임계값, 프롬프트 등 **동작을 바꾸는 값**은
**사용자가 요청했거나 버그 수정에 필수인 경우에만** 변경한다.
변경할 때는 보고에 반드시 세 가지를 명시한다:

| 항목 | 내용 |
|---|---|
| **(a) 변경 전후 값** | `JUDGE_MAX_TOKENS 16000 → 32000` 처럼 정확히 |
| **(b) 변경 이유** | 어떤 버그의 어떤 증상을 막기 위한 것인지 |
| **(c) 영향 범위** | 무엇이 함께 바뀌는지 (비용·지연·실패 모드·다른 호출 경로) |

**"성능이 나아질 것 같아서" 같은 자발적 튜닝은 금지한다.**

> **왜**: 2026-08-25, judge가 도구 호출을 못 내는 문제를 고치면서 `max_tokens`를
> 16000 → 32000으로 **임의 상향**했다. Anthropic SDK가 그 크기의 비스트리밍 호출을
> *"Streaming is required for operations that may take longer than 10 minutes"*로
> 거부해 **judge 전 배치가 실패**했고, 전 슬레이트가 "판정 실패"로 나갔다.
> 원래 문제(페이로드 과대)는 배치 축소로 풀어야 했다. 튜닝은 고치는 것처럼 보이지만
> 검증되지 않은 변경이며, 실패 모드를 하나 더 만든다.

- **사양 변경이 필요하면 임의로 바꾸지 말고 보고한다.** 규율 문서와 코드가 어긋나면
  코드를 몰래 맞추지 말고 어긋난 사실을 먼저 알린다.
- **실패는 원인을 특정할 때까지 추정으로 덮지 않는다.**
  > **왜**: "429 레이트리밋 탓"으로 추정했으나, 실제 원인은 **200 OK 산문 응답**이었다.
  > 추정으로 덮었으면 진짜 버그는 그대로 남았을 것이다.
- **새 기능은 테스트와 함께 커밋.** 테스트 없는 기능 코드 커밋 금지.
- **모든 외부 HTTP 호출은 지수 백오프 3회 재시도 + 타임아웃** 필수.

---

## 5-2. 조사 규율 — 추측으로 결론짓지 않는다

### 왜 이 규율이 필요한가

2026-08-26 하루 동안 **추측으로 낸 결론이 8건 연속 틀렸다.** 전부 "코드를 읽고
그럴듯하게 추론"한 것이었고, 실제로 측정했을 때 뒤집혔다. 그중 둘은 사용자가
"실패라는데?"·"진행률?"이라고 물어봐서야 드러났다.

| # | 추측한 결론 | 측정 결과 |
|---|---|---|
| 1 | 삼진이 빠져 허용 xwOBA 부호가 뒤집혔다 | 두 방식 평균차 **0.00020**, 삼진은 원래 포함 |
| 2 | `fetch_expert_picks`가 쿼터 집계에서 누락 | app/ 안에 **호출처 없는 죽은 코드** |
| 3 | 프리페치가 신선도 게이트를 우회한다 | `force_refresh`는 **카드 캐시만** 건드림 |
| 4 | 로컬 빌드 성공 = 배포 성공 | `.gitignore` 때문에 배포 빌드 **2회 실패** |
| 5 | `redeploy`가 새 설정을 반영한다 | **30분간** 엉뚱한 프로세스가 돌고 있었음 |
| 6 | Statcast 캐시 없으면 λ 0/6 | 실제 **5/6** (리서치 폴백 작동) |
| 7 | 배포 SUCCESS면 의도한 것이 실행 중 | **스케줄러가 봇을 실행**하고 있었음 |
| 8 | 타선 잡음이 신호를 덮는다 | 절제 실험 결과 **선발이 주범** |

추가로 `features.py`의 반환 키·컬럼명을 추측해 백테스트가 **3회 연속 실패**했다.
함수 본문을 열어 본 뒤에야 통과했다.

### 5-2-1. 결론 전에 반드시 밟을 순서

1. **코드 본문을 연다.** 함수 시그니처·반환 키·컬럼명·상수·기본값은 추측 금지.
   `grep`으로 이름만 보지 말고 **본문을 읽는다**(1번 사고: 컬럼 의미를 확인하지 않음).
2. **데이터·로그로 재현하거나 측정한다.** 상관계수·분포·사분위·절제 실험 중
   최소 하나로 수치를 낸다.
3. **외부 지표·라이브러리 정의가 걸리면 웹 검색으로 확인한다.**
   학습 데이터 기억으로 단정하지 않는다(지표 정의·API 계약은 자주 바뀐다).
4. 그 다음에 결론을 말한다.

### 5-2-2. 보고 문구 규칙

- **"~때문일 것이다"·"아마"·"~로 보인다"를 원인 보고에 쓰지 마라.**
  추정은 **다음 조사 항목**이지 결론이 아니다.
- 측정하지 않았으면 **"아직 모른다"**고 쓴다. 모른다는 보고가 틀린 확신보다 낫다.
- 틀렸던 추측은 **정정을 먼저** 쓴다. 새 결론을 앞세우고 정정을 뒤에 묻지 않는다.

### 5-2-3. 상태값은 증거가 아니다

`SUCCESS` · `200 OK` · `테스트 통과` · `배포 완료`는 **의도한 것이 실제로 동작한다는
증거가 아니다.** 반드시 로그와 실데이터로 확인한다.

- 배포 상태 SUCCESS → **로그에서 실제 실행 명령을 확인**(7번 사고)
- 200 OK → **본문이 데이터인지 확인**(RESEARCH_VALIDATION.md의 산문 응답 사고)
- 테스트 통과 → **배포 컨텍스트에서도 되는지 확인**(4번 사고)

### 5-2-4. 절제 실험을 먼저 하라

어떤 요소가 문제인지 모를 때, **하나씩 빼고 재측정**하는 것이 추론보다 빠르고 정확하다.

실측(2026-08-26 λ 백테스트, 1,496경기):

| 구성 | 정확도 | Brier |
|---|---|---|
| 항상 홈팀 (기준) | 52.67% | 0.2500 |
| 현행 (타선+선발+구장) | 46.26% | 0.2640 |
| 타선 제거 | 47.51% | 0.2640 |
| **선발 제거** | **51.67%** | **0.2515** |

절제 실험 한 번이 그날의 추론 세 번보다 정확했다.

---

## 6. 배포 규율 — 재시작 없이는 아무것도 도달하지 않는다

### 6-1. 코드 수정 후 반드시 봇·스케줄러를 재시작한다

**커밋과 테스트 통과는 배포가 아니다.** 실행 중인 프로세스는 기동 시점의 코드를
메모리에 들고 있으며, 파일을 고쳐도 그 프로세스는 바뀌지 않는다.

```bash
pkill -f "app.bot"; pkill -f "app.scheduler"
PYTHONPATH=. nohup uv run python -m app.bot       > logs/bot.log       2>&1 &
PYTHONPATH=. nohup uv run python -m app.scheduler > logs/scheduler.log 2>&1 &
```

**실사고 (2026-08-25)**: 봇이 8/24 07:34에 기동된 뒤 재시작 없이 하루 넘게 돌았다.
그 사이 26커밋 — λ 포아송 모델, Statcast 수집기, 라인무브먼트, 판정 철학 전면 교체,
시스템 상수 — 이 전부 반영되지 않았다. 사용자는 새 기능이 동작한다고 믿었지만
실제로는 구버전 답을 받고 있었다. 동시에 스케줄러는 **아예 실행되지 않아**
프리페치·채점·배당 스냅샷·라인업 폴링 8개 잡이 모두 멈춰 있었고,
그 결과 픽 11건이 전부 미채점 상태로 쌓였다.

### 6-2. 작업 종료 시 `/health`로 확인한다

`/health`는 다음을 한 화면에 보여준다:
- 실행 중인 **커밋 해시**와 최신 대비 뒤처진 커밋 수
- **스케줄러 하트비트**(2분 주기) — 없으면 스케줄러가 죽은 것이다
- 잡별 마지막 실행·다음 실행 시각
- API 잔여 쿼터, 마지막 프리페치 결과, 오늘 λ 가동률·판정 성공률

**"고쳤다"고 보고하기 전에 `/health`에서 내 커밋이 도는지 확인하라.**
KBO·NPB 저녁 발송은 `/checklist`와 [docs/PREGAME_CHECKLIST.md](PREGAME_CHECKLIST.md)로 시계·사고(작동/미작동)·연구 항목을 대조한다.

### 6-3. 조용한 실패 금지

프리페치는 성공·실패와 무관하게 **항상 실행 리포트를 발송**한다([7-1]).
단계가 실패하면 그 시점에 즉시 알린다([7-2]) — 원인 분류·영향 한 줄과 함께.
결함이 있는 리포트는 카드 상단에 **한계를 반드시 표시**한다([7-4]).

**실사고 (2026-08-25)**: 프리페치가 λ 0/15, judge 0/15, 축구 크래시로 사실상
전부 실패했는데 사용자는 아무것도 통보받지 못했다. 로그에만 남고 끝났다.

---

## 부록: 코드-규율 불일치 현황 (2026-08-25 기준)

| 규율 | 코드 상태 | 조치 |
|---|---|---|
| 1-A-2 야구 2-소스 폐기 | 추천 게이트에서 야구 2-소스 미적용 | 2026-08-29 사용자 지시 |
| 1-A-2 NPB last-3 검증 전 추천 탈락 | `npb_last3_verified=True` | 2026-08-29 라이브 36/36 후 True |
| 1-B-6 라운드 5 미만 가드 | 라운드 조건 미구현 (리그 성숙도·실데이터 유무로 대리) | 축구 — 라운드 수집 경로 확보 후 |
| 2-3 교차검증 | MLB만 대상, 축구 미대상 | football-data 순위표 기반 확장 검토 |
| 5-1 파라미터 임의 변경 금지 | 코드로 강제 불가 (리뷰·보고로만) | 변경 시 (a)전후값 (b)이유 (c)영향 3종 보고 |

## 부록: 파라미터 변경 이력

변경할 때마다 여기에 한 줄 남긴다 — 되돌릴 때 무엇을 되돌려야 하는지가 남아야 한다.

| 날짜 | 파라미터 | 전 → 후 | 이유 | 결과 |
|---|---|---|---|---|
| 2026-08-25 | `JUDGE_MAX_TOKENS` | 16000 → 32000 | judge가 사고에 토큰을 다 써 tool_use 미출력 | ❌ **실패** — SDK 비스트리밍 거부로 전 배치 실패. 16000으로 롤백 |
| 2026-08-25 | `JUDGE_MAX_TOKENS` | 32000 → 16000 | 위 롤백 | ✅ 정상 |
| 2026-08-25 | `JUDGE_BATCH` | (신규) → 4 | 10경기 단일 호출이 토큰 초과 → 배치 분할이 근본 해법 | ✅ 요청 7경기 → 판정 7경기 |
| 2026-08-25 | `RESEARCH_CONCURRENCY` | 4 → 2 | Perplexity 429 방어 (사용자 지시 "동시 1~2로 축소") | ✅ 429 0건 |
| 2026-08-25 | Perplexity 1차 프롬프트 | 금지문 추가 | 사용자 승인 | ⚠️ 결장 8→20건 개선, 재요청 1→5회 증가 |
| 2026-08-25 | `JUDGE_MAX_TOKENS` | 16000 → 32000 → 16000 | SDK 비스트리밍 거부로 즉시 롤백 | ✅ |
| 2026-08-25 | `JUDGE_BATCH` | 5 → 4 | 배치 실패 후 여유 확보 | ✅ 7경기 전건 판정 |
| 2026-08-25 | `ensemble_w_*` | 0.45/0.30/0.25 → 0.50/**0.00**/0.50 | **사용자 지시** — 시장 배제 | 판정 철학 교체 |
| 2026-08-25 | `min_win_prob`/`min_odds` | (신규) 0.58 / 1.60 | **사용자 지시** — EV 기준 폐기 | ⚠️ EV −7.2% 조합 허용 |
| 2026-08-25 | `adj_*` (11종) | (신규) | **사용자 지시** — 경기력 조정 계수 | 결장·불펜이 확률에 반영됨 |
| 2026-08-28 | `disabled_providers` | (없음) → `grok,perplexity` | **사용자 지시** — AI API는 Anthropic만 충전. Grok·Perplexity는 의도적 미사용 | 호출 0 · 알림 0 |
| 2026-08-28 | 크레딧 알림 창 (`quota:*`) | 30분 → **KST 자정까지(하루 1회)** | **사용자 지시** — 30분 창이 배당 스냅샷 주기와 같아 밤새 반복 발송. 인증 알림은 30분 유지 | 이미 차단된 곳은 0건 |
| 2026-08-28 | `lineup_lead_npb` | 1.0시간 → **0.5시간** | **사용자 지시** — NPB 라인업은 경기 30분 전 (KBO는 1시간 전 유지) | |
| 2026-08-28 | `lineup_final_minutes_kbo/npb` | (없음, 공통 30분) → **60 / 30** | **사용자 지시** — 최종 라인업 창을 공시 시각에 맞춤 | |
| 2026-08-28 | `NPBStopBefore` | 20분 → **10분** | **사용자 지시** — 17:45 발송보다 먼저 끊으면 Yahoo 타순이 스냅샷에 없음 | |
| 2026-08-28 | `NPBStopBefore` | 10분 → **2분** | **사용자 지시** — 1차 이후 사람이 바꾼 타순을 잡아 재발송. T-10 중단은 T-15 목표·변동 재발송과 충돌 | |
| 2026-08-28 | `NPBStopBefore` / `NPB_FINISH_MIN` | 2분 → **15분** | **사용자 지시** — NPB 크롤·분석은 17:45에 끝낸다. 18:00 경기 T-15. 그 이후 NPB 재판정 없음 | |
| 2026-08-28 | `disabled_providers` | `grok,perplexity` → `grok,perplexity,groq,gemini` | **사용자 지시** — 오늘 Groq·Gemini 건너뛰고 Claude만 | 해석·서술·의도가 Anthropic으로 직행 |
| 2026-08-28 | `intent_fallback` | `gemini` → `gemini,anthropic` | **사용자 지시** 부수 — groq·gemini 미사용 시 의도 파싱 체인이 비지 않게 | |
| 2026-08-28 | `deepsearch_sports` | `mlb,soccer` → **`soccer`** | **사용자 지시** — MLB를 KBO·NPB와 같이 공식 API 완결. 딥서치·Odds·휴리스틱 삭제 | MLB 프리페치가 Perplexity를 안 부름. 추천은 배당 없어 0건이 정상 |
| 2026-08-28 | `lambda_h2h_min_edge` | (없음) → **0.05** | **사용자 지시** — 승패 λ가 동전 던지기면 Claude를 깎지 말 것. 언더오버 불변. `min_win_prob` 0.58 유지 | |
| 2026-08-29 | `BULLPEN_RECENT_DAYS` | 달력 3일 → **최근 3경기** (`BULLPEN_RECENT_GAMES=3`) | **사용자 지시** — MLB 불펜 창을 KBO usage와 같게. 키 `bp_pitches_3d` 유지 | λ 불펜 항의 표본 창이 바뀜. 더블헤더·휴식일에 달력 3일과 어긋나던 값 |
| 2026-08-29 | `physical.LIMITS["rank"]` | (1, 12) → **(1, 15)** | **사용자 지시** — MLB 리그 순위 13–15위를 파싱 오류로 버리지 말 것. KBO/NPB 1–12는 그대로 통과 | 중하위 MLB 팀 순위 칸이 빈칸이 되지 않음 |
| 2026-08-29 | `pregame_push.SPORTS` / `SEND_OPEN_MIN["mlb"]` | KBO·NPB만 / (없음) → **+MLB / 180분** | **사용자 지시** — MLB도 KBO·NPB와 같은 자동 발송. 창은 `lineup_lead_mlb` 3시간과 같음 | 아침 05–11 KST 캐시 발송. `min_win_prob` 0.58 유지 |
| 2026-08-29 | 야구 Odds 조회 | 호출 안 함 → **totals 라인만** | **사용자 지시** — 승부는 배당 무관, 언더오버는 라인 숫자만 | h2h를 요청·적재하지 않음. 승률·추천 자격에 배당 0. `min_win_prob` 0.58 유지 |
| 2026-08-29 | 야구 언더오버 | 보드·Odds 라인 → **삭제** | **사용자 지시** — λ 토탈 적중 불신. 승패·런라인·F5 유지 | 야구 Odds 0 |
| 2026-08-29 | 픽 채점기 | grade_date·성적표 → **삭제** | **사용자 지시** — 목표는 적중. 채점 회로 불필요 | 13:00 잡은 종료 점수 적재만. `min_win_prob` 0.58 유지 |
| 2026-08-29 | 야구 2-소스 | data+model 필수 → **폐기** | **사용자 지시** — 폼·라인업 판정. 축구 2-소스는 유지 | 야구 추천 = 58%(원정 63%)+라인업. 확신도 하/패스는 거부권 |
| 2026-08-29 | 야구 +20%/25%p 플래그 | 이상치 탐지 → **야구 삭제** | **사용자 지시** — 배당을 안 봄. 축구는 유지 | |
| 2026-08-29 | `npb_last3_verified` | (없음) → **False** | **사용자 지시** — NPB 3경기 수집 검증 전 추천 자동 탈락 | 보드만 발송. MLB·KBO 먼저. True는 라이브 측정 후 |
| 2026-08-29 | `npb_last3_verified` | False → **True** | **사용자 승인** — standings 배선 후 라이브 36/36 부착, 타격표 대조 일치, 전 필드 누락 0 | NPB도 58%+라인업이면 추천 가능 |
| 2026-08-29 | `MODEL_TEAM_FORM` / `team_form_max_tokens` | (Judge `claude-opus-4-6` · `JUDGE_MAX_TOKENS` 16000) → **`claude-haiku-4-5-20251001` / 1500** | **사용자 지시** — 팀 분석은 요약·태그 분류. 코드에 모델 ID 하드코딩 금지 | 폼 경로만. Judge(--old) 불변. temperature 0 (extra_body) |
| 2026-08-29 | `MODEL_MATCHUP` / `matchup_max_tokens` | (동일 Judge 경로) → **`claude-sonnet-5` / 1000** | **사용자 지시** — 매치업은 4자료 교차·규칙 준수. 폼과 모델 분리 | 매치업 경로만. Judge(--old) 불변. temperature 0 |
| 2026-08-29 | NPB/MLB 등판 백필 창 | 14일 → **21일** (`APPEARANCE_DAYS`) | **사용자 지시** — 오늘 선발 최근 2~3등판. 14일×로테이션이면 n=1로 끊김 | 타순 이력 상한(10경기)과 분리. 폼·매치업 모델 불변 |
