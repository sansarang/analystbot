# FINDINGS 전수 점검 — 남은 것과 순서 (2026-09-08 23:4x)

사용자 지시: *"파인딩에서 수정해서 할게 무엇인지 전부다 수정후 알려주라."*
계획서 §ⓐ(타자 축)와 §ⓑ(재판정 게이트)를 끝낸 뒤의 상태다.

## 0. 세는 방법 — 그리고 세면서 알게 된 것

`FINDINGS.md`(10,736줄)는 항목 표기가 두 가지다 — `- **ID [등급] 제목**` 과
`## 🔴 ID [등급] 제목`. 둘 다 긁으면 **고유 ID 421개**다.

⚠️ **이 숫자를 "남은 결함 421개"로 읽지 마라.** 같은 결함이 여러 절에
   다시 나오고(예: DSP-1 이 3곳), 이미 고쳐진 것도 문서에는 그대로 있다.
   아래는 **내가 실제로 확인한 것만** 상태를 붙였다.

```
고유 ID            421
이번 감사에서 처리   23   (커밋으로 추적 가능한 것)
그 중 배포됨        18   (09-08 16:5x · 17:3x)
미배포             5+8  (feature/v1.5-model — 09-09 정오 예정)
```

---

## 1. 🔴 배포 전에 사용자 결정이 필요한 것

### FRZ-1 동결이 안 풀렸다 — 그런데 내가 풀렸다고 적었다

시스템 자신의 카운터(`daily_summary.freeze_progress_lines`)와 **같은 쿼리**로
운영 실측:

```
FREEZE_START = 2026-09-07 (세 리그 공통, 표본 재시작)
mlb 10/50 · kbo 0/50 · npb 0/50      ← 미충족
```

내가 BAT-4 커밋에 적은 "kbo 158 · mlb 369 · npb 130" 은 **행 단위**였고
재시작일을 무시한 수였다. 정정했고(`2def77c`), BAT-6(`2561fae`)이 자료3
주입을 **기본 꺼짐** 게이트 뒤로 옮겼다.

**결정할 것 — 둘 중 하나다.**

| 갈래 | 뜻 | 대가 |
|---|---|---|
| ① 표본을 채우고 켠다 | `BATTER_MATERIAL_ENABLED` 를 리그별 50건 뒤에 켠다 | KBO·NPB 는 열흘 안팎 대기. 그동안 자료3 은 지금 그대로 |
| ② 동결을 명시적으로 해제하고 켠다 | 지금 켠다 | 표본이 그 시점부터 다시 시작된다. `FREEZE_RESTART_IS_FINAL = True` 와 충돌하므로 **그 상수부터 다뤄야 한다** |

⚠️ 어느 쪽이든 **수집·적재는 지금부터 돈다** — 켜는 날 표본이 0이면 켜는
   의미가 없기 때문이다.

---

## 2. 09-09 정오 배포 대상 (`feature/v1.5-model`, 8커밋)

| 커밋 | 무엇 | 위험 |
|---|---|---|
| `08b7a40` ELO-1 | MLB 시즌 백필 (자료12 를 실력으로) | **적재 후 되돌리기 어렵다.** GM-3 이 먼저 배포돼야 안전하다 |
| `0135a2e` GM-3 | 병합 창 UTC 날짜 → ±2시간 | 이것이 ELO-1 의 전제다 |
| `f9e16ef` BAT-1 | `batter_appearances` 표 + MLB 파서 | 표 추가 |
| `ef18a9f` BAT-2 | KBO·NPB 파서 | 읽기만 |
| `5b4c025` BAT-3 | 세 리그 백필에 배선 | 적재 시작 |
| `6eb2cd0` BAT-4 | 최근 5경기 집계 + 자료3 주입 | 🔒 **게이트 뒤** |
| `69b7df7` BAT-5 | 분기점 개별 타자 해결사 | 라우팅 변경 — ⑨에서 분포를 잰다 |
| `2561fae` BAT-6 | 동결 게이트 | 꺼짐이 기본 |

⚠️ `648849b` RJG-1(라인업 확정만으로는 재판정 안 함)은 **이미 배포돼 있다**.

**배포 순서** — 스케줄러 먼저(기동 시 스키마 적용). 그다음 ELO-1 백필을
스케줄러 1회성 태스크로 돌린다(로컬 맥에 의존하지 않는다).

---

## 3. 확인한 것 — 상태가 바뀐 항목

| 항목 | 문서의 주장 | 재측정 (2026-09-08) | 결론 |
|---|---|---|---|
| **VL-1** | 변수 98.2%가 임계 미기재 → 채점 불능 | 715건 중 **645건(90.2%)** 기재 · 630건 채점 | ✅ **해소** (VARP-1) |
| **ODD-1** | 수집 배당이 h2h 뿐 | `h2h 17,129 · totals 8,932 · spreads 8,456` | ✅ **오래된 기록** |
| **LIVE-2·LLM-5** | 무료 판정 사슬 후보가 1개 | `gemini/…,groq/qwen/…` **2개** | ✅ **해소** (09-08 17:3x) |
| **FRZ-1** | 동결 진행률 0/50 | mlb **10**/50 · kbo 0 · npb 0 | 🔴 **여전히 참** |
| **E-1** | `market_edge` 를 아무도 안 부른다 | 운영 호출부 **0** (테스트만) | 🔴 **여전히 참** |
| **ORP-1** | `glass_report` 운영 호출부 0 | 운영 호출부 **0** (테스트만) | 🔴 **여전히 참** |
| **SC-1** | `league_era` 인자를 만들고 안 쓴다 | `_suppression` 이 인자를 풀어놓고 루프는 `s.league_era` 사용 | 🔴 **여전히 참** |
| **NAR-1** | 서술 입력에 배당·EV | `narrator.py:184` 가 `odds`·`ev` 를 payload 에 넣는다 (136행은 쓰지 말라고 지시) | 🔴 **여전히 참** |
| **SIT-1** | `_norm` 이 단어경계 공백을 지운다 | `re.sub(r"\s+"," ").strip()` — 양끝 공백이 사라진다 | 🔴 **여전히 참** |

⚠️ 나머지 항목은 **재측정하지 않았다.** 문서에 있다는 이유로 "지금도 참"이라고
   쓰지 않는다 — 위 아홉 중 셋이 이미 낡은 기록이었다.

---

## 4. 이번 작업에서 새로 나온 항목

| ID | 무엇 | 왜 중요한가 |
|---|---|---|
| **CACHE-1** | `analysis:*` 가 판정을 3키(`model`·`p_home`·`우세`)만 남긴다 | 전개·분기점·근거가 캐시에 없어 **사후 검증이 불가능**하다. 오늘 BAT-5 의 반대 위험을 못 잰 직접 원인 |
| **BF-1** | "타순 파싱 실패가 투수 기록까지 버린다"가 MLB·KBO 에 남아 있다 | NPB 는 2026-09-01 에 고쳤고 BAT-3 이 **타자 쪽만** 우회했다 |

---

## 5. 다음에 할 일 — 권하는 순서

**① 배포와 실측이 먼저다.** 8커밋을 배포하고 ⑨(첫 사이클 실측)를 붙이기
전에는 다음 수정을 시작하지 않는다. 특히 ELO-1 백필은 `games` 를
357 → 2,500+ 로 키우므로 `merge_duplicate_games`·`finals_job`·λ·
`asia_poll_window` 가 처음 겪는 규모다.

**② 그다음은 "쓰이지도 않는데 살아 있는 것"을 정리한다.** 값은 작지만
위험도 작고, 지도가 선명해진다 — E-1(`market_edge` 미배선) ·
ORP-1(`glass_report` 미배선) · SC-1(`league_era` 미사용).

**③ 그다음이 진짜 문제다 — PL-1.**

> 우리 LLM 판정 AUC 0.470~0.519 · 브라이어 0.2530
> 단순 Elo(전 시즌)  AUC 0.558
> 시장               AUC 0.654

**판정이 단순 Elo 보다 못하다.** 자료를 더 넣는 것(BAT 시리즈)은 재료 쪽
처방이고, PL-1 은 **산출 쪽** 처방이다 — `p_final` 을 LLM 숫자 그대로 쓰지
않고 Elo 에 앵커링하거나 섞는 것. 축구는 이미 `0.50*p_model + 0.50*p_claude`
로 그렇게 한다. 야구만 앙상블이 없다.
⚠️ 이것은 **확률 게이트·클립과 같은 동결 대상**이라 §1 의 결정과 묶인다.

**④ CACHE-1 을 그 앞에 둘 값이 있다.** ③을 재려면 판정 전문이 남아 있어야
한다. 지금은 `p_home` 하나만 남아서 "왜 그 확률이 나왔는가"를 사후에 못 본다.

---

## 6. 정직하게 남기는 것

- **⑨ 미기록**: GM-2(실제 병합 필요) · PGP-2(보장선 실패 사례 필요) ·
  RJG-1 · ELO-1 · GM-3 · BAT-1~6(배포 전).
- **MISTRAL_API_KEY**: 값은 비웠지만 **변수 항목 자체는 남아 있다.**
  콘솔에서 옛 키를 폐기해야 유출이 무효가 된다.
- **SEC-1(a)**: 키 3개는 교체했지만 **각 콘솔에서 옛 키 폐기**가 남았다.
- FINDINGS 의 나머지 ~390 항목은 **재측정하지 않았다.**
