# LE1-ROI — 내가 만든 거짓 ROI (영향 지도 5문)

## ① 무엇이 틀렸나 (실측 2026-09-22 · 소급 적재 378행)

```
result   행수    배당 있는 행
win       208        69      ← 139건이 ROI 계산에서 빠졌다
loss      158        42      ← 그런데 158건 **전부** −1 로 세어졌다
void        2         1

첫 표: ROI −0.4656 (n=229)   n = 69(win) + 158(loss) + 2(void)
```

`roi_unit` 이 패배를 **배당 없이도** `−1.0` 으로 세고, 승리는 배당이 없으면
버렸다. 분모가 승·패에서 다르면 ROI 는 **반드시 음수로 치우친다.**

🔴 **틀린 방향이 더 나쁘다.** 숫자가 나빠 보이는 쪽으로 틀렸으므로
   "엔진이 안 된다"는 결론을 만든다. LE-2 의 기준선이 될 표였다.

## ② 테이블 / 잡

없다. `app/learning/metrics.py` 의 순수 함수 하나.
⚠️ 이미 적재된 `decision_ledger.roi_unit` 칸에도 같은 거짓 값이 들어갔다 —
   **재적재**로 덮는다(`backfill_from_picks` 는 `ON CONFLICT DO NOTHING` 이라
   덮지 않는다 → 그 칸만 UPDATE 하는 것이 아니라 **표를 비우고 다시 넣는다**.
   행 수가 378 로 같은지 확인한다).

## ③ 깨질 테스트

`test_roi_는_단위_스테이크다` 가 `{"result": "loss", "price_at_decision": 1.80}`
만 보므로 깨지지 않는다. 전체 스위트로 확인한다.

## ④ 롤백

함수 한 곳. 되돌리면 거짓 값으로 돌아간다.

## ⑤ 완료 조건

- 계약 3건 통과(지금 실패 → 통과)
- **첫 표를 다시 낸다.** ROI 의 `n` 이 `69+42+1 = 112` 로 줄고 값이 바뀐다
- 전체 스위트 통과 · 발송 0건
