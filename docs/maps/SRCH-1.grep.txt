### ① 읽는 곳 전부 — xsearch
app/config.py:185:    scout_xsearch_daily_cap: int = Field(default=12, validation_alias=AliasChoices(
app/config.py:186:        "SCOUT_XSEARCH_DAILY_CAP", "scout_xsearch_daily_cap"))
app/config.py:190:    scout_xsearch_rss_floor: int = Field(default=5, validation_alias=AliasChoices(
app/config.py:191:        "SCOUT_XSEARCH_RSS_FLOOR", "scout_xsearch_rss_floor"))
app/config.py:193:    scout_xsearch_lineup_min: float = Field(
app/config.py:195:            "SCOUT_XSEARCH_LINEUP_MIN", "scout_xsearch_lineup_min"))
app/config.py:197:    scout_xsearch_enabled: bool = Field(default=False, validation_alias=AliasChoices(
app/config.py:198:        "SCOUT_XSEARCH_ENABLED", "scout_xsearch_enabled"))
app/config.py:204:    scout_xsearch_situation: bool = Field(
app/config.py:206:            "SCOUT_XSEARCH_SITUATION", "scout_xsearch_situation"))
app/registry.py:116:    #: 뉴스 소스. `rss` 는 무료 상시, `xsearch` 는 조건부(캡 있음).
app/registry.py:131:    ScoutSport("kbo", ("rss", "xsearch"), lineup_source="naver_kbo"),
app/registry.py:132:    ScoutSport("npb", ("rss", "xsearch"), lineup_source="yahoo_npb"),
app/registry.py:133:    ScoutSport("mlb", ("rss", "xsearch"), lineup_source="statsapi"),
app/pipeline.py:2604:async def _maybe_xsearch(jg: dict, date: str, redis, table: dict) -> dict:
app/pipeline.py:2610:       AI 발 항목은 `source="xsearch"` 라벨로 구분된다(1주 기여도 비교용).
app/pipeline.py:2612:    from app.collectors.xsearch import fetch_for_game, should_fire
app/pipeline.py:2631:    logger.info("[scout] xsearch 판별 game=%s 발동=%s (%s) enabled=%s",
app/pipeline.py:2632:                jg.get("game_id"), fire, why, s.scout_xsearch_enabled)
app/pipeline.py:2633:    if not fire or not s.scout_xsearch_enabled:
app/pipeline.py:2643:    logger.info("[scout] xsearch 적재 game=%s +%d건 (source=xsearch)",
app/pipeline.py:2686:                _tbl = await _maybe_xsearch(_g, date, r, _tbl)
app/pipeline.py:2688:                logger.warning("[xsearch] %s 층 실패 — RSS 만으로 진행: %s",
app/engine/gather.py:10:     · `xsearch.fetch_for_game` — 질문 없는 속보 수집기가 **놀고 있었다**
app/engine/gather.py:91:    """X 속보. **`xsearch` 가 원본이다** — 프롬프트·URL 검증·캡을 다시 만들지 않는다.
app/engine/gather.py:95:    ⚠️ 경기당 1콜·일일 캡은 `xsearch` 안에 있다. 우회하지 않는다.
app/engine/gather.py:97:    from app.collectors.xsearch import fetch_for_game, load_cache
app/engine/gather.py:101:    # 🔴 **먼저 캐시를 읽는다.** `xsearch` 는 경기당 1콜이라, 스케줄러가 낮에
app/engine/gather.py:108:    #    `title` 로 **바꿔서** 돌려준다(xsearch.py:150). 실측 2026-09-12:
app/engine/deepsearch.py:1098:    ⚠️ `xsearch.fetch_for_game` 을 쓰지 않는다 — 그쪽은 **일반 속보**를 긁는
app/engine/verify.py:97:    · X          — `collectors.xsearch.fetch_for_game`(경기당 1콜·일일 캡)

### ① 읽는 곳 전부 — verify(2차 검증)
app/config.py:449:    verify_enabled: bool = Field(default=False, validation_alias=AliasChoices(
app/config.py:450:        "VERIFY_ENABLED", "verify_enabled"))
app/config.py:454:    verify_model: str = Field(default="claude-sonnet-5",
app/config.py:456:                                  "VERIFY_MODEL", "verify_model"))
app/config.py:710:    #   "2차만 안트로픽 사용하고 관련없는 거는 빼라."
app/llm/provider.py:176:#    "2차 판정만 안트로픽 사용하고 관련없는 거는 빼라."
app/llm/judge_route.py:36:#   ⚠️ 이 역할은 `chain()` 을 타지 않는다. `verify` 가 `verify_model` 로 직접 부른다.
app/engine/form_card.py:302:        if ov.get("2차") == "반대":
app/engine/form_card.py:303:            lines.append(f"   ⚠️ 2차 검증 이견 — {ov.get('2차사유')}")
app/engine/form_card.py:304:        elif ov.get("2차") in ("검증실패", "없음"):
app/engine/judge.py:60:                            "description": "2차 반박 검증에서 발견된 실체적 반전 요인 (한국어, 근거 수치 포함). 없으면 빈 문자열",
app/engine/matchup.py:1021:    jg["verify"] = ver
app/engine/matchup.py:1029:        "2차": ver["판정"], "2차사유": ver["사유"],

### ① grok 의 x 도구
app/research/grok.py:115:        (/v1/responses + web_search·x_search 툴) 사용. 날짜 필터는 프롬프트로 제약.
app/research/grok.py:156:    #: [ORD-18 2026-09-12] 검색 도구. 🔴 **기본은 x_search 전용이다.**
app/research/grok.py:161:    #   web_search 2~3회 · x_search **0회**. 우리가 원한 X 게시물을 안 찾았다.
app/research/grok.py:166:    #      x_search 수율은 이미 흔들린다(같은 프롬프트 5회에 답 [0,0,1,0,1]).
app/research/grok.py:167:    #      **지우지 않고 옵트인으로 남긴다** — 웹이 꼭 필요한 경로는 web=True.
app/research/grok.py:168:    X_ONLY = [{"type": "x_search"}]
app/research/grok.py:169:    WEB_AND_X = [{"type": "web_search"}, {"type": "x_search"}]
app/research/grok.py:171:    def _tools(self, web: bool) -> list[dict]:
app/research/grok.py:172:        return self.WEB_AND_X if web else self.X_ONLY
app/research/grok.py:181:                "tools": self._tools(web),
app/research/grok.py:193:        🔴 왜 필요한가. 진단 실측 2026-09-12: x_search 는 정상 작동 중인데
app/research/grok.py:194:           (`x_search_calls` 9 · `web_search_calls` 0) 우리가 받은 본문은
app/research/grok.py:205:                "tools": self._tools(web),
app/research/grok.py:246:    """호출 하나의 도구·토큰·비용을 한 줄로. 🔴 이것이 없어서 오늘 x_search
app/research/grok.py:257:    logger.info("[grok] 도구=%s 입력=%s 출력=%s x=%s web=%s $%.4f",
app/research/grok.py:259:                d.get("x_search_calls"), d.get("web_search_calls"),

### ① 테스트
tests/test_scout_registry.py
tests/test_anthropic_final_only.py
tests/test_xsearch_path.py
tests/test_verify.py
tests/test_gather.py
tests/test_situation.py
tests/test_order_v3.py
