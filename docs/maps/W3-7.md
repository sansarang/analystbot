# W3-7 — 순위표 (착수 · **설계 전 실측**)

`wiring_first_0920` 의 남은 항목. 사용자 지시 2026-09-21: "착수하고 배포해줘"

## ① 지금 상태 — 실측

```
standings 표          **없다**(information_schema 조회)
순위가 사는 곳         전부 **메모리**
   MLB   pipeline.py:196  client.fetch_standings() → parse_standings → research
   KBO   naver_kbo.build_standings(…)               → research
   NPB   yahoo_npb.standings()
   축구   football.fetch_standings(competition)      (football-data)
```

캐시의 채움률:
```
종목      경기   순위 칸 있음
mlb        3         3     ✅
npb        1         0     ❌
kbo        0         0     (캐시에 research 자체가 없다)
soccer     0         0
```

## ② 🔴 여기서 멈췄다 — 표본이 **4건**이다

"표가 없다"는 사실이고, "순위가 메모리에만 있다"도 사실이다.
그런데 **"표가 없어서 무엇이 비는가"를 4건으로는 말할 수 없다.**

지금 스키마를 정하면:
- 종목 4개의 **생산자 모양이 다르다**(MLB dict · KBO build_standings ·
  NPB HTML · 축구 football-data JSON). 넷을 한 커밋에 넣으면 무엇이 틀렸는지
  못 가린다 — 오늘 이 이유로 여러 번 쪼갰다.
- 어느 칸이 **실제로 필요한지** 모른다. 승·패·무·승점·순위·게임차 중
  판정이 쓰는 것이 무엇인지 4건으로는 안 보인다.

⚠️ **추측으로 표를 만들지 않는다.** 그건 오늘 하루 내내 피한 것이다.

## ③ 그래서 순서를 이렇게 본다

1. **배포**(29커밋 · 묶음 6개) — 캐시가 다시 찬다
2. 첫 사이클 뒤 **채움률을 다시 잰다**(종목별 n≥20)
3. 그 실측으로 **필요한 칸만** 정하고 W3-7 을 연다
4. 생산자는 **한 종목씩** 잇는다

## ④ 지금 확실히 말할 수 있는 것

- 🔴 NPB 가 1/1 비어 있다 — 표본이 얇지만 **0 이라는 사실**은 남는다
- 🔴 KBO·축구는 **research 자체가 캐시에 없다** — 순위 이전의 문제다
  (KBO 는 오늘 소스를 되살렸고(RES-1), 축구는 DS-3 로 검색을 갈았다.
   배포 뒤 이 둘이 어떻게 변하는지가 먼저다)
- ✅ MLB 는 3/3 채워진다 — 메모리 경로가 **동작은 한다**

## ⑤ 착수 결과

**설계 문서까지.** 코드 0 · 테이블 0.
표본이 설계를 지지하지 않아 **멈추고 적었다.**
