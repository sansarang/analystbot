# DS-2a — 직독기를 "변경 감지"로 (영향 지도 5문)

사용자 지시 2026-09-21: "순서대로 해라" (내 제안 2순위 = DS-2a)

## ① 무엇이 틀렸나 — 오늘 실제로 놓친 셋

| 시각 | 무슨 일 | 우리가 안 때 |
|---|---|---|
| 08:30 | 마린스 18:00 세이부전 **중지**(태풍) | 한참 뒤 |
| 11:06 | 라쿠텐 등록·말소 **공시** | 한참 뒤 |
| 12:40경 | 라쿠텐-소프트뱅크 **우천 중지** | 13시 넘어 |

🔴 **셋 다 검색 실패가 아니라 타이밍 실패다.** 페이지는 그 자리에 있었고
   우리가 그 순간에 안 봤을 뿐이다. 검색 API 를 사도 이건 안 고쳐진다.

🔴 그리고 그날 나는 11:50 기사("예정대로 개장")를 보고 **"개최"라고 보고했다.**
   그 뒤 스포츠나비가 `試合中止 降雨のため` 를 띄웠다. 상태 사실에 시각이
   없어서 **낡은 사실이 새 사실을 이겼다.**

## ② 무엇을 만드나

| 파일 | 무엇 |
|---|---|
| `app/deepsearch/watch.py` | `content_hash`·`interval_min`·`StatusFact`·`lag_minutes`·`Watcher` |
| `config/deepsearch.yaml` | `watch:` 블록 — 주기·잡음 문턱·stale 분 |
| `db/schema.sql` | `watch_events` |
| `tests/deepsearch/test_addendum.py` | T-ADD 1~6 · 12~14 |

**기존 수집기 수정 0.** 새 경로를 만들 뿐이다.

## ③ 사본이 생기나

- 요청은 **전부 DS-1 런타임**을 지난다. `watch.py` 는 httpx 를 import 하지
  않는다 — 계약 `test_모든_외부_요청이_런타임을_지난다` 가 잠근다.
- 주기·문턱은 `config/deepsearch.yaml` 하나.
- ⚠️ `ignore_selectors` 의 원본은 `config/source_map.yaml` 이다(DS-2 가 채운다).
  `watch.py` 는 받아 쓸 뿐 자기가 목록을 갖지 않는다.

## ④ 🔴 T-ADD 14 를 좁혔다 — 숨기지 않는다

지시문 원문은 "프록시 회전·UA 위장 목록·Tor/SOCKS·쿠키 주입이 **코드·설정에
없다**"를 저장소 전체에 요구한다. 그런데 이미 있다 — `tor_search.py` ·
UA 위장 22파일(D34) · 그것들이 실제로 돈다(DS-0 계측: `check.torproject.org` 6회).

이 갈림길에서 **사용자가 "위장 거부 끄지 마라"를 골랐다**(2026-09-21).
그래서 계약을 **`app/deepsearch/` 로 좁혔다** — 새로 만드는 경로는 깨끗하게
가고, 기존 경로는 그대로 둔다. 이 좁힘이 곧 "규율이 문서에만 남는" 쪽임을
기록해 둔다(F-18 옆 갈림길, 충돌 1).

## ⑤ 기존 경로를 건드리나

- 0건. 아직 스케줄러 잡으로 걸지 않았다 — **이을 자리는 DS-2** 다.
- ⚠️ 또 "만들어 놓고 안 이음" 상태다. 의도한 것이고 적어 둔다.

## ⑥ 틀렸을 때 누가 알려주나

T-ADD 1~6: 잡음만 바뀌면 변화 아님 · 파서는 바뀐 때만 · ETag/304 ·
T-180/T-65/T-20 경계 주기 · **as_of 없으면 사실이 아님 · 최신이 이김 ·
30분 뒤 stale** · 인지 지연(분) · 매번 바뀌는 페이지 제외 + 사유.
T-ADD 12~14: 제3자 크롤러 없음 · 브라우저 조종 없음 · 딥서치 경로에 우회 없음.
