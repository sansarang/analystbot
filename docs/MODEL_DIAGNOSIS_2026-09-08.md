# 예측 진단 — 2026-09-08 (v1.5 조사)

> `FINDINGS.md` 의 ①~⑤(LED-A·GATE-2·GATE-3·PL-1·LED-B)를 **운영 원장으로 직접
> 재측정**한 기록이다. 감사 수치를 그대로 옮기지 않았다. 코드는 아직 안 고쳤다.

## 0. 방법 — 경기 단위로 센다

`pick_ledger` 는 **경기당 여러 행**이다(채점행 645 / 경기 147, 경기당 4.4행).
재판정이 많은 경기가 그만큼 여러 번 세어지므로 행 단위 집계는 재판정 횟수로
가중된다. `is_final` 이 경기당 1행을 보장한다
(`idx_pick_ledger_final(game_id,date) WHERE is_final` 유니크 부분 인덱스).

⚠️ **시스템 자체 보고는 정상이다** — `calibration.py:105`·`daily_summary.py:132`
   가 이미 `l.is_final` 로 거른다. 틀렸던 것은 **감사 문서(LED-A)와 이 세션의
   첫 백테스트**였다.

| | 추천 적중률 |
|---|---|
| 행 단위 (LED-A 가 쓴 방식) | 51.4% |
| **경기 단위 (`is_final`)** | **29.4%** |

이 차이 때문에 첫 백테스트에서 `p>=0.58 · |괴리|<4 → 75.9%` 라는 수를 냈다가
경기 단위로 64.3%(n=14)로 무너지는 것을 확인했다. **그 수는 폐기한다.**

## 1. 게이트가 역방향이다 (LED-A 재확인, 더 나쁨)

```
157경기 · 기준 54.8%
  보드만      113경기  58.4%
  거부권탈락    22경기  63.6%     ← 버린 것
  추천         17경기  29.4%     ← 고른 것
  가치주의       5경기  20.0%
```
추천이 기준보다 **25.4%p 낮다**(감사는 11.1%p 로 봤다). 표본 17이라 단독으로는
유의하지 않지만 아래 §2 가 구조를 설명한다.

## 2. 확률 띠가 뒤집혀 있다 (GATE-3 재확인)

```
p 0.50–0.55  45경기  71.1%
p 0.55–0.60  54경기  40.7%   ← 추천 하한 0.58 이 이 띠 안에 있다
p 0.60–0.65  45경기  48.9%
p 0.65–0.70  13경기  76.9%
```
0.50–0.55 와 0.55–0.60 의 격차가 **99경기에서 30.4%p**. 가장 단단한 신호다.
**임계를 옮기는 것으로는 안 고쳐진다 — 띠 자체가 뒤집혀 있다.**

뿌리는 PL-1 이다: `p_final = p_claude` 원값이고 보정도 앙상블도 없다
(docstring 은 `0.5*p_model + 0.5*p_claude` 라고 적혀 있지만 야구 분기는
`blend()` 를 부르지 않는다).

## 3. 🆕 재판정이 판정을 **나쁘게** 만든다 — 감사에 없던 것

세 축이 같은 방향이다.

```
잠정            27경기  70.4%
확정(재판정함)   130경기  51.5%          (18.9%p)

재판정 1회 69.0%(29) · 2회 65.0%(40) · 3회 46.4%(28) · 5회+ 41.4%(29)

우세가 뒤집힌 11경기 — 최종이 맞은 것 4 · 1차가 맞은 것 7
```

⚠️ 앞의 두 축에는 **선택 편향**이 있다 — "잠정 27경기"는 라인업이 끝내
   확정되지 않은 경기라 성격이 다를 수 있다. 그러나 **세 번째 축은 같은 경기
   안에서의 비교라 편향이 없고**, 방향이 같다.

FINDINGS 의 LED-1(경기당 4.9회)·SCH-2(재판정 방아쇠 `changed` 가 넓다)와
같은 뿌리로 보인다.

## 4. NPB — 약점이 홈 우세에 몰려 있다

```
npb 홈우세   14경기  28.6%        실제 홈승률: npb 51.9%(131) ← 3리그 최저
npb 원정우세   9경기  55.6%                   kbo 54.3% · mlb 54.2%
```
모델이 NPB 23경기 중 14경기를 홈 우세로 찍는데 NPB 는 홈 이점이 가장 약하다.
그 14경기 중 4경기만 맞았다(이항 p≈0.04, 표본 14라 결정적이지 않다).

## 5. 판정 모델별 (참고 — 표본이 작다)

```
kbo  claude-sonnet-5 68.4%(19) · gemini 20.0%(5) · nemotron 40.0%(5)
mlb  claude-opus-5 66.7%(15) · gemini 62.5%(16) · sonnet 56.7%(67) · grok 42.9%(7)
npb  claude-sonnet-5 42.9%(14) · gemini 28.6%(7)
```

## 다음 (사용자 결정)

1. **보정은 섬도로 먼저** — 원장으로 보정 곡선을 만들어 `p_calibrated` 를
   **기록만** 하고 게이트·카드·발송은 그대로 둔다. 1~2주 뒤 단조성을 재고
   그때 반영한다. (이 저장소의 감시 3층이 전부 섬도인 것과 같은 방식)
2. **NPB 는 원인 조사 우선** — 위 §4 가 실마리다. 표본이 작아 더 봐야 한다.
3. **§3(재판정 악화)은 새 항목** — 편향 없는 축(뒤집힘 11건)을 표본이 쌓이면
   다시 잰다.
