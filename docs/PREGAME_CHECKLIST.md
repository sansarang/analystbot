# KBO·NPB 저녁 발송 점검

첫 확인: **2026-08-29 KST**. 매일 같은 시계로 본다.
18:00 NPB / 18:30 KBO가 기본이다. 시작 시각이 다르면 T-15·T-40으로 환산한다.

근거 커밋: `a76f809`. `/health` 해시가 이것(또는 이후 커밋)인지 먼저 본다.
텔레그램 명령: **`/checklist`** — 계약 + 지금 측정값. **`/health`** — 커밋·잡·크롤러.

**17:30에 카드가 있어야 한다고 코드가 보장하지는 않는다.** 타순이 늦게 뜨면 카드도 늦다. 강제 종료선은 NPB **17:45(T-15)** 뿐이다.

---

## 계약 (이게 아니면 실패한 것)

- [ ] 17:45에 KBO+NPB를 **한꺼번에** 덤프하지 않는다
- [ ] 카드 헤더는 `1차` 또는 `변동`이다. `17:45 예측`이 아니다
- [ ] 캐시·`p_claude` 없으면 **빈 카드를 보내지 않는다**
- [ ] 14:00 아시아 프리페치는 텔레그램을 보내지 않는다
- [ ] MLB·축구 자동 발송 없음
- [ ] 선발·타순이 바뀌면 `변동`으로 다시 보낸다 (NPB는 17:45 전만)
- [ ] NPB 크롤은 17:45에 끊는다 (`NPBStopBefore` 15분)
- [ ] NPB 재판정은 17:45 이후 하지 않는다
- [ ] 이미 시작한 경기는 보내지 않는다

---

## 내일 시계 (KST)

### 14:00
- [ ] 프리페치 리포트만. 경기 예측 카드 없음
- [ ] `/health`에 `prefetch_asia` 기록이 생긴다

### 16:50
- [ ] `/health` 코드가 `a76f809`(또는 이후)
- [ ] 스케줄러 하트비트 살아 있음
- [ ] 크롤러 정상
- [ ] `asia_pregame_5m` 다음 실행이 **오늘 17:00** (내일이 아님)

### 17:00
- [ ] `asia_pregame_5m` 마지막 실행이 17:00대
- [ ] 아직 타순 없으면 카드 없음 (정상). 슬레이트 파이프라인을 저녁에 돌리지 않음

### 17:20
- [ ] 발송 창 열림 (NPB T-40, KBO T-70)
- [ ] 타순+판정이 있는 경기는 이때부터 나가도 된다

### 17:25–17:44 (NPB 타순이 뜨는 구간)
- [ ] 타순이 뜬 **그 경기만** 카드가 나온다. 6경기를 12분 묶음으로 기다리지 않는다
- [ ] 헤더 `⏰ NPB · 1차`
- [ ] 로그: `재판정 game=` 과 `예측 카드 sent`
- [ ] 같은 타순으로 5분마다 또 보내지 않는다

### 17:45 종료선 (NPB 18:00 → T-15)
- [ ] 크롤러 NPB 가속이 끊긴다
- [ ] 이후 로그에 `NPB T-15 이후 라인업 변동 — 재판정 안 함` 만 있고 새 Judge가 없다
- [ ] 이미 판정된 미발송 카드는 시작 전까지 나가도 된다
- [ ] 17:45 이후 바뀐 NPB 타순은 **카드에 안 반영**된다 (의도된 포기)

### 17:45–18:00
- [ ] `분석 캐시가 없어` 카드 0건
- [ ] 시작 시각이 지난 경기는 스킵

### 18:00 이후 (KBO 18:30)
- [ ] NPB는 시작했으므로 발송 없음
- [ ] KBO는 시작 전까지 분석·변동 재발송 가능
- [ ] 18:30 지난 KBO는 스킵

---

## 로그에서 볼 문자열

| 보이면 통과 | 보이면 실패 |
|---|---|
| `[pregame] npb game=… sent` | `오늘 분석 캐시가 없어` |
| `[pregame] … revised` (타순 변경 시) | `⏰ KBO · 17:45 예측` |
| `NPB T-15 이후 라인업 변동 — 재판정 안 함` (17:45 이후) | 17:45 이후 `[judge] mode: LIVE` 로 NPB 재판정 |
| `asia_pregame_5m` | `pregame_push_1745` |

확인: 텔레그램 관리자 채팅 + 스케줄러 로그 + `/checklist`.
