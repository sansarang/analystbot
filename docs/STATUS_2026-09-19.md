# AnalystBot 현재 상태 정직 보고 — 2026-09-19

지시문 `status_report_for_ops` 의 항목 A~H 를 §1 형식(상태 · 증빙 원문 · 한계)으로 답한다.
측정 2026-09-19 09:45~10:10 KST · 운영 컨테이너/DB/Redis/로그를 직접 읽었다.
규율: 추측 금지 · 원문 증빙 · 사본 금지 · 모르면 "모른다" · 코드 변경 제안은 §7(H)에만.

---

# §8. 1쪽 요약

```
[AnalystBot 상태 요약 — 2026-09-19 10:10 KST]

오늘처럼 운영(매일 T-3h export → 페이블 판정 → 원장 태그)이 지금 가능한가:
  MLB : 예   — 오늘 실제로 했다. 6개 핵심 필드가 15경기 전건 채워졌다.
  KBO : 아니오 — 선발·결장·라인업 전부 없음. 나오는 것은 배당과 경기뿐.
  NPB : 아니오 — 같은 이유. 불펜만 일부(6경기 중 3경기) 나온다.
  축구: 아니오 — 위 셋에 더해 ELO 자체가 없다(키 0개).

지금 확실히 되는 것 (증빙 있음):
  1) MLB export — 배당 전 마켓·마진제거·선발 last3·불펜 3일·결장·form·ELO 15/15
  2) 카드 발송 차단 — dispatch:mlb:2026-09-18 = {'pipeline_v14': '777'}
  3) 관측 루프 — flow_shadow_15m 이 15분마다 36경기, analysis_runs 684행/run 214

지금 안 되는 것 (증빙 있음):
  1) ①사전값 — 오늘 날짜 ELO 키가 없어 최근 사이클 36경기 전건 보드고정
  2) ⑤수집 — 게이트를 통과한 7경기도 n05_evidence=[] , 6변수 전부 unknown
  3) ⑪구조픽 — model_probs 1,382행 전건 NULL → 파생 후보가 0
  4) 판정 LLM 1순위 gemini — HTTP 429 prepayment credits depleted
  5) "페이블 판정" 태그 — 그런 칸이 없다(structure_pick 0/1382)

모르는 것 (측정 안 됨):
  1) 봇 서비스가 지금 실제로 응답하는가 (마지막 로그 09-18 10:12 KST 이후 조용)
  2) 컨테이너의 for_fable.py(880줄)와 로컬 HEAD(881줄) 1줄 차이의 내용
  3) KBO·NPB 라인업이 오늘 0인 것이 "아직 시각이 아니라서"인지 "소스가 없어서"인지
  4) bullpen_3d 가 DB 를 직접 읽는데도 n05 에서 0행이던 이유

라인업 확정을 잡는 시각: 킥오프 −2.50h ~ −3.92h (오늘 MLB 15경기 실측 · 중앙 −3.17h)
export 자동화 상태: 수동 (전용 잡 없음 — python -m app.export.for_fable)
LLM: v1.4 경로에서 LLM 이 필요한 노드 = n12_text 하나뿐 / 살아있는 제공자 = groq (HTTP 200)
스위치: PIPELINE_V14=True  SEND=False  스케줄러=살아있음(8ff9ce16, 09-19 08:58 KST 배포)
```

## P0-A. `lineup_history` 소급 적재 — 리그별 건수 (09-14 [41] 에 대한 답)

- 상태: 됨 (적재는 끝났다)
- 증빙 원문:

  ```
  칸: id, game_id, team_id, player_id, player_name, started, minutes,
      lineup_type, kickoff_utc, captured_at, league, ccode, kickoff_date
  전체 18,507행 · 39리그

  Championship  960   DFB Pokal 929   EFL Cup 918   League Two 863
  National League 839   J. League 2 800   LaLiga 779   Serie A 726
  J. League 720   League One 720   LaLiga2 660   National League North 645
  Premier League 560   K-League 1 560

  우리가 운영하는 리그만:
    LaLiga 779 · Serie A 726 · Premier League 560 · Ligue 1 558 · Bundesliga 280
  ```
  `player_id` 칸이 있다 — 09-14 [38] "이름 매칭 금지, 선수 id 기준"의 전제가 갖춰져 있다.
- 한계: 39리그 중 대부분은 우리가 운영하지 않는 하위 리그다. **야구(MLB·KBO·NPB)는
  이 테이블에 0행**이다 — 축구 전용 적재다. 야구 라인업은 `lineups`(MLB 303경기 ·
  NPB 6경기 · KBO 0경기)에 따로 있다.

## P0-B. 미상 도메인 일일 보고 · tier 승격 후보 (09-14 [35])

- 상태: 부분 — **로그는 매일 남지만 "보고"는 존재하지 않는다**
- 증빙 원문: 로그는 오늘도 찍혔다 (2026-09-19 10:04~10:06 KST)

  ```
  [scout] bundesliga pre — 미상 1건(후보 ['hessenschau.de']) · 그중 fetch ['hessenschau.de']
  [scout] epl pre — 미상 16건(후보 ['bbc.co.uk','bbc.com','blog.ke.sportpesa.com',
          'chroniclelive.co.uk','fantasyfootballscout.co.uk','fubo.tv','goal.com',
          'hulldailymail.co.uk']) · 그중 fetch ['bbc.com','tntsports.co.uk']
  [scout] epl pre — 미상 30건(후보 [...]) · 그중 fetch 없음
  [scout] la_liga pre — 미상 2건(후보 ['mundodeportivo.com','radionervion.com'])
  ```
  기록하는 코드 `app/engine/scout_config.py:392`:
  ```python
  # 🔴 [SCT-9] 목록에 없는 도메인을 **매일 남긴다** — tier 승격 후보다.
  logger.info("[scout] %s %s — 미상 %d건(후보 %s) · 그중 fetch %s", ...)
  ```
  그 기록을 **읽어서 모으는 곳**:
  ```
  $ rg -n "unknown_domains|미상 도메인|scout:unknown|domain_seen" app/ tools/
  (출력 없음)
  ```
- 한계: 마지막 "보고" 시각은 **없다** — 그런 보고가 만들어진 적이 없다.
  `bbc.com` · `goal.com` · `mundodeportivo.com` 처럼 매일 반복해서 미상으로
  잡히는 도메인이 있는데 tier 표에 올라가지 않는다. 집계 없이 로그만 흘러간다.

## P0-C. `report.py` 호출부 (09-17 [149])

- 상태: 안 됨 — **파일 자체가 없다**
- 증빙 원문:

  ```
  $ ls -la tools/report.py
  ls: tools/report.py: No such file or directory

  $ rg -n "report\.py|tools\.report" --glob '!*.jsonl' .
  ./FINDINGS.md:6661: ORP-1 [최상] `glass_report.py` 508줄 — **운영 호출부가 0건**이다
  (코드 호출 0건. FINDINGS.md 의 언급뿐)
  ```
- 한계: 09-17 에 "원장에서 행을 뽑아 넣는 도구가 없다"고 적힌 상태 그대로다.
  `app/engine/glass_report.py`(508줄)가 있으나 운영 호출부가 0건이라는 것도
  `FINDINGS.md:6661` 에 이미 기록돼 있다.

## P0-덧. 측정 중 발견한 어긋남 (등록만 — 고치지 않았다)

위성 로그가 v1.4 흐름과 **다른 게이트 값**을 쓰고 있다:

```
10:21 KST  [scout] Hanwha Eagles@LG Twins — 게이트 대상(가치 의심) 으로 추출한다
10:21 KST  [scout] Doosan Bears@KT Wiz — 게이트 미판정 · 빅매치 아님 · LLM 추출 생략
```
같은 시각 v1.4 흐름은 KBO 4경기 전건을 `보드고정`(사전값 없음)으로 찍었다(D-3).
즉 위성은 구경로(`analysis_cache`/`pick_ledger`)의 게이트를 읽고, 흐름은 자기
`n03` 을 쓴다 — 한 슬레이트에 게이트가 둘이다. `Phase 0-b` 로 등록한다.

---

# §0. 편집·배포·컨테이너 변경 0건

```
$ git status --short
?? docs/STATUS_2026-09-19.md
```

추적 파일 수정 0 · 커밋 0 · 배포 0. 목록의 한 줄은 이 보고서 자신이다.
컨테이너에서 실행한 것은 `SELECT` · `scan_iter` · 읽기 전용 export 커맨드 · API 프로브
각 1회뿐이고, 모두 `/tmp` 에만 썼다.

---

# §2. 항목 A — 실행 상태

## A-1. 서비스 3개

- 상태: 됨 (셋 다 SUCCESS)
- 증빙 원문:

| 서비스 | 배포 ID | 상태 | 배포 시각(KST) | 마지막 정상 사이클(KST) |
|---|---|---|---|---|
| analystbot-scheduler | `8ff9ce16-7381-47fb-a8ef-a8077a9e3de8` | SUCCESS | 09-19 08:58 | 09-19 09:44 |
| analystbot-bot | `ac9781e3-9e0f-460b-9a55-25203ca6f98b` | SUCCESS | 09-17 20:53 | 09-18 10:12 (마지막 로그) |
| analystbot-crawler | `0ef9bc7b-73a6-4c98-b021-4214360498d2` | SUCCESS | 09-17 20:53 | 09-19 10:07 |

  ```
  스케줄러  INFO:app.flow.bridge:[flow] 슬레이트 36경기 · 멈춤 {'n03_freeze': 36} · 발송 0 (예산 0/55)
            Job "flow_shadow_job (... next run at: 2026-09-19 09:59:07 KST)" executed successfully
  크롤러    2026/09/19 10:07:17 [npb] 2026-09-19 — 6경기 수집, 값 30개(폐기 0), 변화 0건, 예정 6
            2026/09/19 10:04:30 [kbo] 2026-09-19 — 4경기 수집, 값 32개(폐기 0), 변화 0건, 예정 4
  봇        2026-09-18T01:12:37Z INFO:aiogram.dispatcher:Connection established (tryings = 4, bot id = 8917065606)
  ```

  컨테이너가 스스로 보고한 커밋: `GIT_COMMIT_SHA = 330da2f94f91612aedd7c3dba7658540d0d138e0`
  로컬 HEAD 와의 차이:
  ```
  $ git log --oneline 330da2f..HEAD
  3d4d153 EXP-1 STEP 2·3·4 — 블록 채움 · MD 요약 · 검증. finish 스냅샷도 고쳤다.
  90e8bf2 EXP-1 STEP 1 — 페이블 내보내기 스켈레톤 (읽기 전용 수동 커맨드).
  ```

- 한계: 봇은 롱폴링이라 이벤트가 없으면 로그를 남기지 않는다 — 09-18 10:12 이후
  조용한 것이 "죽었다"인지 "할 일이 없었다"인지 로그만으로는 갈리지 않는다. 모른다.

## A-2. 스케줄러 잡 목록

- 상태: 됨 (올라가 있다)
- 증빙 원문 (`/app/app/scheduler.py` `_job_specs()`):

  ```
  prefetch_evening        CronTrigger(hour=21, minute=0, KST)
  prefetch_dawn           CronTrigger(hour=4, minute=30, KST)
  prefetch_asia           CronTrigger(hour=14, minute=0, KST)
  triggers_1m             IntervalTrigger(minutes=1)
  watchdog_5m             IntervalTrigger(minutes=5)
  odds_snapshot_30m       IntervalTrigger(minutes=30)
  oddsapinet_2x           CronTrigger(hour="15,17", minute=30, KST)
  satellite_15m           IntervalTrigger(minutes=15)
  flow_shadow_15m         IntervalTrigger(minutes=15)     ← v1.4 관측
  ingest_finals_13h       CronTrigger(hour=13, minute=0, KST)
  soccer_trial_10m        CronTrigger(hour="0-6,18-23", minute="*/10", KST)
  daily_summary_asia      CronTrigger(hour=17, minute=35, KST)
  daily_summary_overseas  CronTrigger(hour=5,  minute=30, KST)
  daily_summary_soccer    CronTrigger(hour=1,  minute=45, KST)
  calibration_weekly      CronTrigger(sun, 23:30, KST)
  research_retry_45m      IntervalTrigger(minutes=45)
  heartbeat_2m            IntervalTrigger(minutes=2)
  ```

  `game_triggers` 는 `triggers_1m` 이 읽는 DB 테이블이고, 최근 행 원문:
  ```
  id=2360 game_id=1767 kind=result          due_at=2026-09-20 08:00Z fired_at=None
  id=2359 game_id=1767 kind=kickoff         due_at=2026-09-20 05:00Z fired_at=None
  id=2358 game_id=1767 kind=close           due_at=2026-09-20 05:00Z fired_at=None
  id=2357 game_id=1767 kind=satellite_stop  due_at=2026-09-20 04:50Z fired_at=None
  ```

- 한계: 잡 목록은 소스의 선언이고, 각 잡이 오늘 실제로 발화했는지는
  `flow_shadow_15m` 하나만 로그로 확인했다. 나머지는 모른다.

## A-3. 환경 스위치 현재값

- 상태: 됨
- 증빙 원문 (운영 컨테이너에서 `get_settings()` 를 직접 읽음):

  ```
  pipeline_v14               True
  pipeline_v14_send          False
  order_v3                   True
  order_v2                   False
  judge_provider             gemini
  judge_chain                gemini/gemini-3.5-flash-lite,groq/openai/gpt-oss-120b
  form_chain                 gemini/gemini-3.5-flash-lite,groq/openai/gpt-oss-120b
  satellite_enabled          True
  websearch_enabled          True
  deepsearch_pplx_enabled    False
  paid_llm_allowed           False
  DEEPSEARCH_ENABLED(env)    true
  DEEPSEARCH_SPORTS(env)     soccer
  SATELLITE_SPORTS(env)      mlb,kbo,npb,soccer
  ```

- 한계: 워치독은 `watchdog_5m` 잡으로 등록돼 있는 것만 확인했다. 경보 코드별
  발화 여부는 재지 않았다 — 모른다.

## A-4. 카드 발송·워치독 경보

- 상태: 안 됨 (의도적으로 막혀 있다)
- 증빙 원문 (운영 Redis):

  ```
  dispatch:mlb:2026-09-18   {'pipeline_v14': '777'}
  dispatch:kbo:2026-09-18   {'window_not_open': '40', 'unjudged': '77'}
  dispatch:npb:2026-09-18   {'window_not_open': '72', 'unjudged': '110', 'already_started': '45'}
  dispatch:mlb:2026-09-17   {'window_not_open': '79', 'unjudged': '432'}
  block:*  0개   budget:*  0개   cost:*  0개
  ```

  `pipeline_v14: 777` 이 v1.4 가드가 카드를 되돌린 횟수다. 09-17 원장에는 그 칸이
  없고 09-18 MLB 원장에는 그 칸만 있다 — 가드가 단일 길목에 들어간 뒤로 다른
  사유가 나오지 않는다.

- 한계: "최근 발송 3건"은 존재하지 않는다 — 09-18 이후 나간 카드가 0건이다.
  차단 키(`block:*`)도 0개라 API 차단 상태는 아니다.

## A-5. 오늘 export 를 누가·언제·어떤 명령으로 만들었나

- 상태: 됨 (수동)
- 증빙 원문:

  ```
  $ ls -la ~/Downloads/analystbot_export/
  -rw-r--r--  245231  9월 19 09:23  2026-09-19_mlb_slate_r3.json
  -rw-r--r--    8321  9월 19 09:23  2026-09-19_mlb_slate_r3.md
  -rw-r--r--   91077  9월 19 09:09  2026-09-19_mlb_slate.json

  export_meta.generated_kst = "2026-09-19T09:22:44.181141+09:00"
  export_meta.git_sha       = "330da2f94f91"
  명령: python -m app.export.for_fable --league mlb --date 2026-09-19
  ```

  만든 주체는 Claude(이 세션)이고, 사용자의 `export_for_fable` 지시로 실행했다.
  `_r2`/`_r3` 접미사는 같은 날 재실행 시 앞 파일을 덮지 않으려고 코드가 붙인다.

- 한계: 자동이 아니다. 스케줄러 잡 목록(A-2)에 export 잡이 없다. 매일 하려면
  사람이 매번 커맨드를 쳐야 한다.

---

# §3. 항목 B — 데이터 공급 능력 (export 필드별)

- 상태: 부분 (핵심 6개는 15/15, 부가 필드 다수가 0/15)
- 증빙 원문: `2026-09-19_mlb_slate_r3.json` 15경기 전수 집계.
  `●`=15/15 · `○`=0/15

| 필드 | 오늘 값 | 소스 | 갱신 주기 | 최신 갱신 KST | 한계 |
|---|---|---|---|---|---|
| odds.ml | ● 15/15 | odds_crawler (draftkings) | 30분(`odds_snapshot_30m`) | 07:25:17 | 북 1곳 스냅샷이다 |
| odds.ah | ● 15/15 | 〃 | 〃 | 〃 | 라인 2개만 |
| odds.total | ● 15/15 | 〃 | 〃 | 〃 | 라인 4개 |
| odds.team_total_home/away | ○ 0/15 | — | — | — | 원문: `운영 MLB 배당 실측 0행 (espn 은 h2h·spreads·totals 만)` |
| odds.f5 | ○ 0/15 | — | — | — | 원문: `F5 배당 수집 경로 없음 (scoring 의 f5 는 확률이지 배당이 아니다)` |
| odds.open_ml | ● 15/15 | odds_snapshots 최초행 | 스냅샷마다 | 07:25 | 개장가가 아니라 우리가 처음 본 값이다 |
| ml_devig / ml_required | ● 15/15 | 계산(`odds_math`) | 즉시 | — | devig 는 합 1.0, required 는 1/odds |
| model_probs.sportradar | ○ 0/15 | — | — | — | 원문: `sportradar 연동 없음 (전수 검색 0건)` |
| model_probs.bot_p_prior / p_code | ○ 0/15 | pick_ledger | — | — | 원문: `이 경기의 원장 행이 없다 (판정 전)` |
| form.last5 (점수·결과) | ● 15/15 | games(final) | 13:00 `ingest_finals_13h` | — | 구장 정보 없음 — `ha`(H/A)와 상대·점수만 |
| starters.name | ● 15/15 | games.home_pitcher | 라인업 폴링 | 04:29 | — |
| starters.confirmed | ○ 0/15 | — | — | — | 원문: `공식 예고 선발 페이지 URL 을 저장하지 않는다` |
| starters.season | ● 15/15 | pitcher_appearances 집계 | 경기 종료 시 | — | 원문: `우리 DB 의 선발 등판 집계다 — 공식 시즌 스탯 API 값이 아니다.` gs 1~6 이므로 **시즌이 아니라 우리가 본 등판 전부**다 |
| starters.last3 (ip·er·h·bb·k) | ● 15/15 | 〃 | 〃 | — | ip 가 `5.666666666666667` 처럼 미정리 |
| starters.last3.pitches | ○ 0/15 | — | — | — | 투구수 미저장 |
| starters.days_rest / cap_flag | ○ 0/15 | — | — | — | `days_rest_from` 날짜는 있다(`2026-09-13`) — 뺄셈만 안 한다 |
| bullpen.last3d / ip_3d_total | ● 15/15 | pitcher_appearances | 경기 종료 시 | — | 투수 10~13명 명단까지 들어 있다 |
| bullpen.era_14d / closer | ○ 0/15 | — | — | — | 원문: `14일 집계 없음` · `마무리 지정 저장 없음` |
| lineup.status / posted_kst | ● 15/15 | statsapi | 폴링 5분 | 04:29 | posted_kst 는 **우리 DB 기록 시각**이지 공식 게시 시각이 아니다 |
| lineup.out (IL basis) | ● 15/15 | absences.py (statsapi IL) | 폴링 | 04:29 | — |
| lineup.out (라인업 제외 basis) | ● 15/15 | 〃 + 확정 라인업 | 〃 | 〃 | 원문 예: `Pittsburgh Pirates의 Rafael Flores Jr.(주전 타자) 라인업 제외로 결장` |
| lineup.doubt | ○ 0/15 | — | — | — | 원문: `MLB Day-to-Day 는 실재하나 statsapi 로스터에 코드가 없다 (12종 전수 실측 · FORKS F-15)` |
| lineup.batting_order_n | ○ 0/15 | — | — | — | 타순 인원수를 안 적는다 |
| venue / park_factor / roof | ○ 0/15 | — | — | — | 원문: `statsapi hydrate=venue(location) 를 받지만 _parse_games 가 무시한다 — 저장 테이블 없음` |
| weather | ○ 0/15 | — | — | — | 원문: `아직 채우지 않음 (STEP 2)` |
| context.gb | ● 15/15 | games(final) 계산 | 13:00 | — | — |
| context.playoff_status / travel | ○ 0/15 | — | — | — | 원문: `진출·탈락 상태 저장 없음` · `시리즈 차수·이동 이력 저장 없음` |
| deepsearch | ● 15/15 (75건) | satellite_articles | 15분 | — | **전건 `var="article"`** — 가설 변수로 분류된 것이 0건이다 |
| v14_run 스냅샷 | ● 15/15 | analysis_runs | 15분 | 09:44 | n08/n11/stop_reason 은 0/15 |

## B-끝. 핵심 6개 필드의 "매일 나올 확률"

- 상태: 모름
- 증빙 원문:

  ```
  analysis_runs 일자별
    2026-09-19  684행 · run 214
    (그 이전 날짜 행 없음)
  ```

- 한계: **7일 로그가 없다.** `analysis_runs` 는 오늘 하루치뿐이고, export 산출물도
  오늘 것만 있다. 하루 표본으로 "매일 나올 확률"을 말하면 그건 측정이 아니라
  추측이다. 오늘 하루에 대해서만 사실로 말할 수 있다 — 6개 전부 15/15.

---

# §4. 항목 C — 리그별 커버리지

## C-MLB

- 상태: 됨
- 증빙 원문: §3 표 (15경기 · 핵심 6필드 15/15).
  배당 크롤: draftkings · h2h/spreads/totals.
  라인업 소스: statsapi (`lineup.home.source = "statsapi"`).
- 한계: 팀토탈·F5 마켓이 없어 페이블이 실제로 건 `NYY 팀토탈 언더 3.5` 같은 픽은
  **봇이 그 마켓의 배당을 갖고 있지 않다.** 오늘은 사람이 다른 데서 봤다는 뜻이다.

## C-KBO

- 상태: 부분 (export 는 만들어지나 내용이 배당뿐)
- 증빙 원문 (지금 실행):

  ```
  $ python -m app.export.for_fable --league kbo --date 2026-09-19
  INFO 내보냄 /tmp/Downloads/analystbot_export/2026-09-19_kbo_slate.json · 4경기 · 51751바이트

  쓴 소스: analysis_runs, games(final), odds_crawler, satellite_articles, team_elo
  ## Hanwha Eagles @ LG Twins · 2026-09-19 17:00 KST
  - 배당 홈 1.75 / 원정 2.0  ·  마진 제거 홈 0.5333 / 원정 0.4667
  - 선발 홈 미정 — 최근 등판 기록 없음
  - 선발 원정 미정 — 최근 등판 기록 없음
  - 불펜 3일 홈 None이닝 / 원정 None이닝
  - 결장 홈 None명(none) / 원정 None명(none)
  - 게이트 보드고정 (None%p)
  ```

  DB 실측: 4경기 · 확정XI 0 · 선발 양쪽 0 · 배당 4/4.
  배당 크롤: Go 크롤러가 10분마다 (`[kbo] 4경기 수집, 값 32개`).
- 한계: 6개 핵심 필드 중 **배당 하나만** 나온다. 라인업 소스는
  `crawler_lineup_poll(("kbo",))` 이 있으나 오늘 10:05 시점 확정 0건이다 —
  아시아 야구 라인업이 보통 T-1h 에 뜨는 것인지 소스가 안 도는 것인지는 재지
  않았다. 모른다.

## C-NPB

- 상태: 부분
- 증빙 원문:

  ```
  $ python -m app.export.for_fable --league npb --date 2026-09-19
  INFO 내보냄 ... 6경기 · 90905바이트
  쓴 소스: analysis_runs, games(final), odds_crawler, pitcher_appearances, satellite_articles, team_elo
  - 배당 홈 1.7 / 원정 2.1  ·  마진 제거 홈 0.5526 / 원정 0.4474
  - 선발 홈 미정 — 최근 등판 기록 없음
  - 불펜 3일 홈 1.0이닝 / 원정 None이닝
  - 결장 홈 None명(none)
  ```

  DB 실측: 6경기 · 확정XI 0 · 선발 양쪽 0 · 배당 6/6.
- 한계: KBO 와 같고, 불펜만 일부 나온다(`pitcher_appearances` 가 소스에 있다).

## C-축구

- 상태: 안 됨
- 증빙 원문:

  ```
  $ python -m app.export.for_fable --league kleague --date 2026-09-19
  INFO 내보냄 ... 2경기 · 23302바이트
  쓴 소스: analysis_cache, analysis_runs, lineups, odds_crawler, satellite_articles
                                                      ↑ team_elo 가 없다
  - 배당 홈 3.0 / 원정 2.2  ·  마진 제거 홈 0.3043 / 원정 0.4149
  - 선발 홈 미정 / 불펜 3일 None이닝 / 결장 홈 0명(none)
  - 게이트 보드고정 (None%p)

  운영 Redis:  elo:* 키 3개 = elo:kbo:2026-09-18, elo:mlb:2026-09-18, elo:npb:2026-09-18
               (축구 키 0개)
  ```

  DB 실측 오늘 축구 26경기: 라리가3·분데스5·세리에A3·EPL5·J1 8·K리그1 2,
  확정XI 0/26, 배당 26/26, `lineups` 행은 21경기에 있다.
- 한계: 두 가지가 겹쳤다. (1) ELO 키가 없어 ①사전값이 원천적으로 불가능하다.
  (2) MD 요약 서식이 야구용이라 축구에 `선발`·`불펜` 줄이 그대로 찍힌다 —
  내용 결함이 아니라 서식 결함이다. 그리고 `lineups` 행 21건과
  `games.lineup_status='confirmed'` 0건이 왜 어긋나는지는 재지 않았다. 모른다.

---

# §5. 항목 D — v1.4 파이프라인 실제 동작 (지난 24h)

## D-1. 노드별 도달 건수 · stop_reason 분포

- 상태: 부분 (①②③는 전건, ④부터 급감, ⑦ 이후 0)
- 증빙 원문:

  ```sql
  SELECT node, count(*) n, count(DISTINCT game_id) g FROM analysis_runs
   WHERE created_at_utc >= now() - interval '24 hours' GROUP BY 1 ORDER BY 2 DESC;
  ```
  ```
  n01_prior      178행 · 47경기
  n02_market     178행 · 47경기
  n03_gate       178행 · 47경기
  n04_hyp         14행 ·  7경기
  n05_evidence    14행 ·  7경기
  n06_verdict     14행 ·  7경기
  (n07~n13 = 0행)

  SELECT snapshot_json->>'stop_reason' ... WHERE node='finish'  →  0행
  ```
  가장 최근 사이클 로그:
  ```
  INFO:app.flow.bridge:[flow] 슬레이트 36경기 · 멈춤 {'n03_freeze': 36} · 발송 0 (예산 0/55)
  ```

- 한계: `stop_reason` 은 `finish` 스냅샷에 담기는데 그 코드가 든 커밋(`3d4d153`)이
  배포되지 않았다(A-1). 그래서 멈춘 자리는 bridge 로그의 `{'n03_freeze': 36}` 로만
  읽힌다 — 항목별 집계는 아직 원장에서 못 뽑는다.

## D-2. n01_prior 는 team_elo 로 계산하나, 원장을 읽나

- 상태: 됨 (team_elo 다. 원장을 읽지 않는다)
- 증빙 원문 `app/flow/nodes/n01_prior.py:44-56`:

  ```python
  async def _load_elo(state, ctx) -> dict:
      if "elo" in (ctx.inject or {}):
          return dict(ctx.inject["elo"] or {})
      if ctx.redis is None:
          return {}
      try:
          from app.models import team_elo as TE
          date = (state.kickoff_utc or "")[:10]
          raw = await TE.load(ctx.redis, _code(state), date)
          return dict(raw or {})
  ```
  `n01_prior.py:72-78`:
  ```python
  if eh is None or ea is None:
      miss = [n for n, v in ((state.home, eh), (state.away, ea)) if v is None]
      logger.info("[flow:n01] game=%s elo 미기입 %s — 사전값 없음", state.game_id, miss)
      state.n01_prior = {"p_home": None, ..., "source": "team_elo", "missing": miss}
  ```

  오늘 gap 이 null 인 정확한 원인 — 세 가지가 맞물렸다:

  1. 캐시 키가 **킥오프 UTC 날짜**다. 오늘 KST 슬레이트는 UTC `2026-09-19` 인데
     Redis 에 그 키가 없다:
     ```
     elo:kbo:2026-09-18  팀 10개 · TTL 26926s
     elo:mlb:2026-09-18  팀 30개 · TTL  1324s
     elo:npb:2026-09-18  팀 12개 · TTL 27031s
     ```
  2. `n01_prior` 에는 **캐시가 비었을 때 다시 만드는 경로가 없다.**
     `app/pipeline.py:2915` 에는 있다:
     ```python
     _elo_map = await _team_elo.load(redis, _sport, date)
     if not _elo_map:
         _elo_map = await _team_elo.refresh(pool, redis, _sport, date)
     ```
  3. 축구는 elo 키가 아예 0개다.

  이 설명이 맞는지 숫자로 교차검증했다 — 킥오프 UTC 가 09-18 인 MLB 7경기
  (`10085`~`10091`)만 사전값이 섰고, 그 7경기가 정확히 `n04_hyp` 가 돈 7경기다.
  오늘 export 의 게이트 분포도 같은 값이다: `보드고정 8 · 시장과대 3 · 가치의심 3 · 동의 1`
  (= 15경기 중 7경기만 게이트가 섰다).

- 한계: 같은 값을 읽는 내보내기 도구는 **정책이 다르다**. `for_fable.py:777` 은
  최대 3일 뒤로 걸어간다:
  ```python
  for back in range(0, 4):
      day = (base - _td(days=back)).isoformat()
      got = await TE.load(rd, sport, day)
      if got: return got, day
  ```
  그래서 export 는 `home.elo 1506.6 / elo_asof "2026-09-18"` 을 적고 흐름은 못 적는다.
  어느 쪽이 옳은지는 기록된 적이 없다 — 갈림길이다(§7 제안 1).

## D-3. 보드고정 비율과 원인 분류

- 상태: 됨
- 증빙 원문:

  ```sql
  SELECT snapshot_json->'n03_gate'->>'gate' gate,
         snapshot_json->'n03_gate'->>'stop' stop, count(*) n
    FROM analysis_runs WHERE node='n03_gate'
     AND created_at_utc >= now() - interval '24 hours' GROUP BY 1,2 ORDER BY 3 DESC;
  ```
  ```
  보드고정   stop=true    164
  시장과대   stop=false     6
  가치의심   stop=false     5
  동의      stop=false     3
  ```
  보드고정 비율 164/178 = **92.1%**.

  원인 분류 — 표본 전건이 한 가지였다:
  ```
  reason 원문:  "사전값이 없다 — 보드 고정"
  ```
  최근 25분 창 36경기 전수:

  | 종목 | 사전값 출처 | p_home | 게이트 | 경기 |
  |---|---|---|---|---|
  | kbo | team_elo | None | 보드고정 | 4 |
  | mlb | team_elo | None | 보드고정 | 4 |
  | npb | team_elo | None | 보드고정 | 6 |
  | soccer | team_elo | None | 보드고정 | 22 |

- 한계: "시장 없음" 과 "|gap|≥12" 로 인한 보드고정은 **표본에서 0건**이었다.
  ②시장은 36/36 다 섰다(§6 C-2). 즉 오늘의 보드고정은 전부 ①사전값 결손이다.
  다른 날에도 그런지는 재지 않았다 — 로그가 오늘치뿐이다.

## D-4. n05_evidence 는 실제로 무엇을 수집하나

- 상태: 안 됨 (게이트를 통과해도 빈손이다)
- 증빙 원문:

  ```
  09-19 05:44 g10091  n05_evidence = []
  09-19 05:19 g10090  n05_evidence = []
  ```
  가설이 요구한 변수(④ 출력):
  ```
  starter_recent3 · bullpen_3d · lineup_out · weather · park_factor · travel_backtoback
  ```
  코드가 각 변수를 어디서 받는지 (`app/flow/nodes/n05_evidence.py`):

  | 변수 | 소스 | 운영에서 채워지나 |
  |---|---|---|
  | starter_recent3 | `ctx.inject["starter_notes"]` (185행) | 아니오 |
  | lineup_out | `ctx.inject["absences"]` (154행) | 아니오 |
  | bullpen_3d | DB `pitcher_appearances` (`_bullpen3d`) | 조회는 하나 표본 2건 모두 0행 |
  | form_recent5 / last3 | DB `games` (`_last3`) | 같은 2건에서 0행 |
  | weather · park_factor · travel_backtoback | 없음 | 아니오 |

  주입하는 쪽(`app/flow/bridge.py:117`)은 하나만 준다:
  ```python
  ctx.inject = {"model_probs": model_by_game.get(...)}
  ```

  오늘 deepsearch 가 트랜잭션 기사뿐이었던 이유 — export JSON 75건 전수 집계:
  ```
  Counter({'article': 75})     ← var 가 전건 "article"
  ```
  `n05` 는 `satellite.read_extract` 가 만든 **8칸 JSON** 을 읽는데(`_extract_box`,
  64~73행), 위성이 남긴 것은 분류되지 않은 기사 원문이다. 즉 기사→칸 추출이
  이 경로에 연결돼 있지 않다.

- 한계: 자료가 DB 에 **없어서** unknown 인 것이 아니다. 같은 DB 로 만든 오늘
  export 는 그 값을 적었다:
  ```
  - 선발 홈 Paul Skenes (시즌 6선발 ERA 3.06) — 2026-09-13 7.0이닝 2자책
  - 불펜 3일 홈 18.0이닝 / 원정 9.0이닝
  - 결장 홈 4명(confirmed) / 원정 1명(confirmed)
  ```
  `bullpen_3d` 가 DB 를 직접 읽는데도 0행이던 이유는 표본이 2건뿐이라 재지
  못했다. 모른다.

## D-5. n06_verdict 의 입력이 무엇인가

- 상태: 됨 (입력은 ④가설과 ⑤수집 둘뿐이다. lineup.out·last3·bullpen 은
  ⑤를 거쳐야만 들어가고, 지금은 못 들어간다)
- 증빙 원문 `app/flow/nodes/n06_verdict.py:37-51`:

  ```python
  async def run(state, ctx):
      hyp = (state.n04_hyp or [{}])[0]
      wanted = {v["var"]: bool(v.get("is_core")) for v in (hyp.get("vars") or [])}
      found = {e["var"]: e.get("value") for e in (state.n05_evidence or [])}
      per_var: dict = {}
      for var in wanted:
          per_var[var] = _judge(found.get(var)) if var in found else UNKNOWN
  ```
  실제 출력:
  ```json
  {"per_var": {"weather":"unknown","bullpen_3d":"unknown","lineup_out":"unknown",
   "park_factor":"unknown","starter_recent3":"unknown","travel_backtoback":"unknown"},
   "verdict":"모름과반","core_refuted":[],"unknown_ratio":1.0}
  ```
- 한계: ⑥은 ⑤가 준 것만 본다. **⑤가 비면 ⑥은 구조상 전건 unknown 이다** —
  ⑥ 자체에는 결함이 없다. 고칠 자리는 ⑤다.

## D-6. n11_value 가 지금 무엇을 계산하나

- 상태: 안 됨 (모든 파생 마켓이 None 을 돌려준다)
- 증빙 원문 `app/flow/nodes/n11_value.py:23-52`:

  ```python
  der  = (state.n02_market or {}).get("derivatives") or {}
  ours = (state.n08_pcode or {}).get("ours_markets") or {}
  for name, book in (("total", ...), ("team_total_home", ...), ("team_total_away", ...)):
      ...
      p_ours = (ours.get(f"{name}_{side}") or {}).get(line)
      if p_ours is None:
          continue                      # 모델 없음 — 추측 금지
  for ah in (der.get("ah") or []):
      p_ours = (ours.get("ah") or {}).get(ah.get("line"))
      if p_ours is None:
          continue
  ```
  `ours_markets` 의 원천은 `model_probs` 인데:
  ```sql
  SELECT count(*) t, count(model_probs) m FROM pick_ledger;
  →  t=1382,  m=0
  ```
  None 을 돌려주는 마켓 목록: `total_over` · `total_under` ·
  `team_total_home_over/under` · `team_total_away_over/under` · `ah` — **전부**.

- 한계: 팀토탈은 애초에 배당도 0/15 다(§3). 즉 `model_probs` 를 채워도 팀토탈
  구조픽은 여전히 설 수 없다 — 두 결손이 겹쳐 있다.

## D-7. n10 재판정 T4/T5 가 오늘 발동했나

- 상태: 안 됨 (한 번도 발동하지 않았다)
- 증빙 원문: `analysis_runs` 에 `n10_rejudge` 노드 행 **0건**(D-1 표).
  코드 `app/flow/nodes/n10_rejudge.py:48-56`:
  ```python
  signals = (ctx.inject or {}).get("rejudge_signals") or {}
  ...
  if signals.get("starter_changed"):
      trigger = "starter_changed"
  elif signals.get("lineup_confirmed"):
      trigger = "lineup_confirmed"
  ```
  `bridge.py:117` 은 `rejudge_signals` 를 주입하지 않는다(D-4 와 같은 자리).
- 한계: 애초에 ⑦~⑬이 0건이므로 ⑩까지 갈 흐름이 없었다. 주입이 없다는 사실과
  흐름이 거기 못 간다는 사실이 겹쳐 있어, 주입만 고치면 되는지는 모른다.

---

# §6. 항목 E·F·G — LLM · 예산 · 원장

## E-1. 제공자별 실제 호출 1회

- 상태: 부분 (3개 중 1개 살아 있다)
- 증빙 원문 (운영 컨테이너에서 각 1회 호출):

  ```
  키 유무: gemini=True groq=True openrouter=True

  gemini      HTTP 429  {"error":{"code":429,"message":"Your prepayment credits are
                         depleted. Please go to AI Studio at https://ai.studio/projects
                         to manage your project and billing. ..."}}
  openrouter  HTTP 401  {"error":{"message":"User not found.","code":401}}

  groq(urllib) HTTP 403  error code: 1010          ← Cloudflare 가 UA 로 막은 것
  groq(httpx)  HTTP 200  {"id":"chatcmpl-faca3fc0-...","object":"chat.completion",
                          "model":"openai/gpt-oss-120b", ...}
  groq models  HTTP 200  {"object":"list","data":[...]}
  ```

  첫 번째 groq 결과(403)는 제 프로브의 HTTP 클라이언트 탓이었다. 앱이 실제로
  쓰는 `httpx` 로 다시 부르니 200 이다. **groq 은 살아 있다.**

  사슬 순서: `gemini/gemini-3.5-flash-lite` → `groq/openai/gpt-oss-120b`.
  1순위가 죽어 있고 2순위가 살아 있다.

- 한계: groq 의 **일일 한도 잔량**은 응답 헤더를 읽지 않아 모른다. openrouter 는
  키가 무효인지 계정이 없는 것인지 401 문구만으로는 갈리지 않는다.

## E-2. v1.4 경로에서 LLM 이 필요한 노드 / 필요 없는 노드

- 상태: 됨
- 증빙 원문 (13개 노드 전수 grep):

  | 노드 | LLM |
  |---|---|
  | n01_prior · n02_market · n03_gate · n04_hyp | 안 부른다 |
  | n05_evidence | 안 부른다 (위성이 **미리** 만든 추출 캐시를 읽을 뿐) |
  | n06_verdict | 안 부른다 (`n06_verdict.py:3` — `규칙은 코드다. LLM 을 부르지 않는다.`) |
  | n07 · n08 · n09 · n10 · n11 · n13 | 안 부른다 |
  | **n12_text** | **부른다** — `n12_text.py:67  from app.llm.provider import complete_text` |

  즉 **LLM 이 필요한 노드는 n12_text(카드 글쓰기) 하나뿐이다.** 승자·확률·확신·
  값 판정은 전부 코드가 한다.

- 한계: `n12_text` 는 지금 0건 실행이다. 그리고 위성 추출(기사→칸)은 노드 밖에서
  LLM 을 쓸 수 있는데, 그 경로의 호출량은 재지 않았다. 모른다.

## F-1. 무료 한도 사용량 (지난 7일)

- 상태: 모름
- 증빙 원문:

  ```
  Redis:  budget:*  0개   cost:*  0개   search:*  0개   quota:*  0개
  DB:     cell_ledger 12행 · variable_ledger 1254행 · expert_ledger 300행 · judge_review 0행
  analysis_runs 일자별:  2026-09-19  684행 · run 214   (그 이전 날짜 없음)
  ```
  오늘 v1.4 경로의 검색 예산은 로그에 있다: `예산 0/55` — 슬레이트 상한 55 중 0 사용.
  (③에서 전부 멈춰 ⑤까지 가지 않았으므로 0이다.)

- 한계: **7일 사용량 원장이 존재하지 않는다.** Redis 에 예산/비용 키가 0개이고
  `analysis_runs` 도 오늘치뿐이다. 지난 7일을 말하려면 지어내야 한다 — 안 한다.

## G-1. pick_ledger 스키마와 최근 행

- 상태: 됨 (스키마는 있다. 다만 24시간째 새 행이 없다)
- 증빙 원문: 81칸.

  ```
  id, game_id, sport, league, date, judged_at, p_home, favored, confidence,
  lineup_status, gate_result, model, odds, market_prob, divergence_pp, edge_status,
  rejudge_count, is_final, final_score, winner, hit, void, graded_at, merged_from,
  trial, confidence_probe, shadow_blend, predicted_side, odds_at_verdict, odds_closing,
  clv, p_market, p_code, adj_pp, llm_winner, llm_level, p_model, model_src, model_w,
  model_gap_pp, p_prior, prior_src, gate_reason, odds_open, move_class, move_reason,
  main_axis, counter_axis, market_view, swap_agree, structure_candidates, analyze_model,
  gate_vs_llm, analyze_failed, promoted_at, finalized_at, cancel_reason, hypothesis,
  confirmed, refuted, unknown_axes, adj_dropped, market_flow, watch_state,
  structure_pick, adj_after, p_code_after, grade_after, clv_line_shift, flow_class,
  cancel_virtual_clv, gate_label, pinnacle_gap, pinnacle_gap_label, gate_gap_pp,
  p_base, placeholder_suspect, adj_evidence, regraded_by, analyze_check, model_probs
  ```

  최근 5행 (`is_final`):
  ```
  g9625 mlb 2026-09-17  p_prior 0.4153  p_code 0.5443  시장 과대  하  home  09-18 04:18
  g9624 mlb 2026-09-17  p_prior 0.5438  p_code 0.5816  동의      하  home  09-18 04:18
  g9623 mlb 2026-09-17  p_prior 0.32    p_code 0.3365  시장 과대  하  away  09-18 01:13
  g9620 mlb 2026-09-17  p_prior 0.3927  p_code 0.4581  시장 과대  하  away  09-17 23:53
  g9622 mlb 2026-09-17  p_prior 0.68    p_code 0.68    동의      중  home  09-17 23:34
  ```
  ```sql
  SELECT count(*) FROM pick_ledger WHERE judged_at >= now() - interval '24 hours';
  →  0
  ```
- 한계: 새 행이 0인 것은 LLM 때문이 아니라 `PIPELINE_V14=True` 가 판정 경로를
  v1.4 로 돌렸는데 v1.4 가 ③에서 멈춰 원장을 안 쓰기 때문이다. 두 원인을
  섞지 말 것.

## G-2. "페이블 판정" 태그를 붙일 수 있나

- 상태: 안 됨
- 증빙 원문:

  ```sql
  SELECT column_name FROM information_schema.columns WHERE table_name='pick_ledger'
    AND (column_name ILIKE '%fable%' OR ... '%source%' OR '%tag%' OR '%by%');
  →  ['regraded_by']

  SELECT count(*) t, count(structure_pick) s, count(regraded_by) rb FROM pick_ledger;
  →  t=1382,  s=0,  rb=6      (regraded_by 6행의 값은 'deepsearch')
  ```
  `%fable%` 로 이름이 걸리는 칸은 0개다. `regraded_by` 는 딥서치 재채점 표시로
  이미 쓰이고 있어 그 자리를 빌리면 두 의미가 섞인다.
- 한계: 무엇을 추가해야 하는지는 §7(H) 에 제안으로만 적었다. 만들지 않았다.

## G-3. 09-15 이후 페이블 채팅 픽이 원장에 있나

- 상태: 없다
- 증빙 원문:

  ```sql
  SELECT pl.game_id, g.home, g.away, pl.date, pl.structure_pick, pl.predicted_side
    FROM pick_ledger pl JOIN games g ON g.id=pl.game_id
   WHERE pl.date >= '2026-09-15' AND (g.home/away ILIKE Samsung|Lech|Gwangju|Yankees|Padres|한화);
  ```
  ```
  g1745 Samsung Lions     vs Lotte Giants       2026-09-15  structure_pick=None  predicted_side=None
  g8307 Minnesota Twins   vs New York Yankees   2026-09-15  structure_pick=None  predicted_side='away'
  g8311 Colorado Rockies  vs San Diego Padres   2026-09-15  structure_pick=None  predicted_side='away'
  (Lech Poznan · 광주 행 없음)
  ```
  `structure_pick` 은 전체 1,382행 중 **0행**이 채워져 있다.
- 한계: 승패 방향(`predicted_side`)이 우연히 같은 행은 있으나, 페이블이 실제로 건
  것은 `NYY 팀토탈 언더 3.5` 같은 **파생 마켓**이고 그 칸은 비어 있다.
  즉 "원장에 있다"고 말할 수 없다. 대전 삼성 승은 09-15 삼성 경기 행이 있으나
  `predicted_side` 가 None 이다.

---

# §7. 항목 H — 알려진 결함·미해결

지난 보고(09-18) 미해결 5건 + 오늘 페이블 지적 4건의 현재 상태.
`제안:` 이 붙은 것은 **실행하지 않았다.**

| # | 항목 | 상태 | 근거 |
|---|---|---|---|
| 1 | gemini 크레딧 소진 | 미착수 | HTTP 429 prepayment depleted (E-1) |
| 2 | openrouter 키 무효 | 미착수 | HTTP 401 User not found (E-1) |
| 3 | `model_probs` 전건 NULL | 미착수 | 1382행 중 0행 (D-6) |
| 4 | 축구 티어 파일 4개 비어 있음 | 미착수 | 축구 elo 키 0개로 재확인 (C-축구) |
| 5 | `starter_recent3` 가 선발 교체 때만 참 | 미착수 | `n05_evidence.py:185` — `starter_notes` 주입 없음 (D-4) |
| 6 | 팀토탈·F5 수집 없음 (페이블 지적) | 미착수 | 15경기 전건 null · reason 원문 있음 (§3) |
| 7 | n01 이 원장에 의존하나 (페이블 지적) | **해당 없음으로 확인** | 원장이 아니라 `team_elo` 를 읽는다 (D-2). 진짜 결함은 **날짜 키 + 폴백 부재**다 |
| 8 | ⑥ 입력 배선 (페이블 지적) | 미착수 | ⑥은 ⑤만 본다. ⑤가 비어 있다 (D-4·D-5) |
| 9 | `season` → `recent6` 라벨 (페이블 지적) | 부분 | JSON 에 `season_note` 로 사실은 적혀 있다(`우리 DB 의 선발 등판 집계다`). 키 이름은 아직 `season` 이다 |

### 제안 1 — ELO 사전값이 오늘 날짜에 없을 때의 정책을 하나로 정한다

지금 한 저장소에 두 정책이 있다: 흐름은 그날 키만 보고 포기하고(`n01_prior.py:50`),
내보내기는 3일 뒤로 걸어간다(`for_fable.py:777`). 갈래는 셋 —
(a) 흐름도 뒤로 걸어간다 (b) 흐름이 `TE.refresh` 로 그 자리에서 만든다
(c) ELO 생성을 슬레이트 시작 전 잡으로 앞당긴다.
CLAUDE.md 규칙상 **내가 고를 것이 아니라 딥서치해서 `docs/FORKS.md` 에 남길 것**이다.
영향이 ①→③→전체이므로 값어치가 가장 큰 한 건이다.

### 제안 2 — ⑤가 찾는 자료를 bridge 가 주입한다

`starter_notes`·`absences` 는 DB·캐시에 이미 있다(오늘 export 가 증명한다).
`bridge.py:117` 의 주입 항목을 늘리는 수정이고 새 수집원은 만들지 않는다.
`rejudge_signals`(D-7)도 같은 자리다.

### 제안 3 — `finish` 스냅샷이 든 커밋(`3d4d153`)을 배포한다

배포하면 `stop_reason` 이 `analysis_runs` 에 남아, 로그를 뒤지지 않아도 멈춘
자리를 원장에서 집계할 수 있다(D-1 의 한계가 사라진다).

### 제안 4 — 축구 ELO 를 만들 수 있는지부터 잰다

`team_elo` 는 우리 `games` 테이블만 쓰고 `MIN_GAMES=10` 하한이 있다. 축구 종료
경기 표본이 그 하한을 넘는 리그가 몇인지 **먼저 재는 것**이 순서다. 표본이 없으면
못 만들고, 그 사실 자체가 답이다.

### 제안 5 — "페이블 판정" 기록 자리

`regraded_by` 는 이미 `'deepsearch'` 로 쓰이고 있어 빌리면 의미가 섞인다.
기록하려면 별도 칸이 필요하고, 그건 **새 컬럼**이므로 사용자 승인 사항이다.
승인 전에는 만들지 않는다.

### 제안 6 — 축구 MD 서식

축구 export 에 `선발 미정` · `불펜 3일 None이닝` 줄이 그대로 찍힌다.
내용이 아니라 서식 결함이다(C-축구).
