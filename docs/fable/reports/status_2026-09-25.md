# 상태 — 2026-09-25 (KST 09:3x)

`/fable-status` 산출물. 읽기 전용.
⚠️ 지시문 보고서가 **아니다** — 다섯 절 형식 계약(`*_report.md`)의 대상이 아니다.

## 커밋 (`git log --oneline -20`)

```
bc2437a CC-1~6 — 지시문·보고·자료를 파일로 오가게 하고, 규율을 훅이 강제하게 한다.
5e56da6 PIPE-1·2 — 어느 게이트에서도 픽이 못 나게 짜여 있었다. 그리고 ③이 흐름을 죽였다.
baad431 PIPE-3·4·5 — 등급이 상쇄된 변수로 섰고, 잡음이 만점이었고, 투수가 타순에 있었다.
0859739 PIPE-6·8 — 픽이 흐름 밖에서 났고, 원장은 종목을 뭉뚱그렸다.
6e6fba6 LAM-2 — 덜 이어진 두 자리. 선발 칸과 캐시 날짜.
9b9ccc4 LAM-1 정정 — 내가 슬레이트를 죽였다. `_SLATE_SQL` 은 `id` 를 준다.
af5c419 LAM-1 — KBO·NPB 에 λ 가 통째로 없었다. 재료는 이미 캐시에 있었다.
6cd0586 NWS-C·A — 부상이라고 무조건 -1 하지 않는다. 그리고 전 종목에 건다.
61d0689 NWS-S — 팀별 RSS 가 이미 있었다. 흐름만 안 읽고 있었다.
ae2b307 D61-3 — 죽은 코드 셋을 걷어낸다. 계약이 초록이라 살아 있는 척했다.
```

## 테스트 (`pytest -q` 마지막 줄)

```
6219 passed, 6 skipped in 157.16s (0:02:37)
```

⚠️ 로컬 Postgres·Redis 가 떠 있어야 한다(`colima start` + `docker compose up -d`).
   안 뜬 상태로 돌리면 인프라 오류 118건이 난다 — 코드 문제가 아니다.

## 운영 — 커밋 대조

| 자리 | 커밋 |
|---|---|
| 로컬 HEAD | `bc2437a` |
| 서버 `GIT_COMMIT_SHA` | `6e6fba6` |

🔴 **서버가 4커밋 뒤처져 있다.** PIPE-1~8 과 CC-1~6 은 **배포되지 않았다.**
   의도한 상태다 — 지시 없이 배포하지 않는다. 지금 MLB 경기도 진행 중이었다.
⚠️ `/health` HTTP 응답은 받지 못했다(공개 도메인 미확인) — 커밋 해시는
   `railway ssh` 로 직접 읽었다. 상태값이 아니라 **해시를 대조**한 값이다.

## 자가 점검 최근 3일

| 날짜 | 결과 |
|---|---|
| 2026-09-23 | 🔴 22건 — `results_pending` 21 · `xi_not_today` 1 |
| 2026-09-24 | 🔴 6건 — `results_pending` 6 |
| 2026-09-25 | 🟢 0건 |

## 커버리지

`docs/COVERAGE_2026-09-21.md` — **4일 전**이 최신이다. 이후 재측정 문서가 없다.

## 원장 규모 (운영 · 읽기 전용 조회)

```
SELECT count(*) FROM analysis_runs   → 66,163
SELECT count(*) FROM decision_ledger →  1,303
```

## 미처리 지시문 (`docs/fable/inbox/`)

없음 — `2026-09-25_cc_collab.md` 는 `done/` 으로 옮겼다.
