.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.serena/cache/
data/

# uv.lock은 **추적해야 한다.** 잠금 파일이 없으면 배포 빌드가 의존성을 고정할 수
# 없고(uv sync --frozen 실패), 로컬과 서버가 다른 버전을 설치하게 된다.
# 실사고(2026-08-26): gitignore에 있어 Railway 업로드에서 빠져 빌드가 깨졌다.
#   Build Failed: "/uv.lock": not found


# 실행 로그는 저장소에 넣지 않는다 — 배포 업로드에도 불필요하고,
# tools/deploy.sh 의 미커밋 차단에 매번 걸린다.
logs/
