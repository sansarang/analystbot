# 리그앙 티어 표 — **사용자가 채운다.**
#
# 🔴 Claude Code 는 이 값을 추측해서 채우지 않는다(지시문 Phase 2-1).
#    골격만 만들고 대기한다.
# 🔴 [U3 2026-09-15] `tier: null` 은 이제 **리그 중앙(3)으로 대체되지 않는다.**
#    사전값 자체가 서지 않고(`p_prior=NULL · prior_src='none'`) 그 경기는
#    보드 고정으로 간다. "모른다"를 "보통이다"로 바꾸지 않는다.
#
# 티어 1=우승권 · 2=상위권 · 3=중위권 · 4=하위권 · 5=승격팀/약체
#
# ⚠️ 키는 **우리 `games` 표기**여야 한다. 리즈 사례(키 "Leeds" vs DB
#    "Leeds United FC")가 그래서 미기입으로 빠졌다.
tiers: {}
